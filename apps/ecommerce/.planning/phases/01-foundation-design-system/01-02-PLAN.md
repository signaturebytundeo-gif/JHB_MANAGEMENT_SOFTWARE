---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/navigation/Navigation.tsx
  - src/app/layout.tsx
  - public/images/hummingbird-logo.svg
autonomous: true

must_haves:
  truths:
    - "User can see navigation bar with logo and 5 menu items (Shop, Our Story, Recipes, Subscribe, Cart)"
    - "Navigation works on mobile with hamburger menu toggle"
    - "Navigation works on desktop with horizontal menu"
    - "Touch targets are minimum 44px on mobile (WCAG compliance)"
    - "Screen readers can navigate menu using aria-expanded"
  artifacts:
    - path: "src/components/navigation/Navigation.tsx"
      provides: "Accessible mobile-first navigation component"
      min_lines: 80
      contains: "aria-expanded"
    - path: "public/images/hummingbird-logo.svg"
      provides: "Jamaica House Brand logo"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/navigation/Navigation.tsx"
      via: "import and render"
      pattern: "import.*Navigation"
    - from: "src/components/navigation/Navigation.tsx"
      to: "useState"
      via: "mobile menu state"
      pattern: "useState.*menuOpen"
    - from: "src/components/navigation/Navigation.tsx"
      to: "aria-expanded"
      via: "accessibility attribute"
      pattern: "aria-expanded=\\{.*\\}"
---

<objective>
Create accessible, mobile-first navigation component with logo, 5 menu items, and responsive hamburger menu.

Purpose: Provide primary site navigation meeting WCAG accessibility standards and mobile-first design
Output: Navigation component integrated into root layout, works on mobile and desktop
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rfmstaff/.planning/PROJECT.md
@/Users/rfmstaff/.planning/ROADMAP.md
@/Users/rfmstaff/.planning/STATE.md
@/Users/rfmstaff/.planning/phases/01-foundation-design-system/01-RESEARCH.md
@/Users/rfmstaff/.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create navigation component with accessibility</name>
  <files>
src/components/navigation/Navigation.tsx
public/images/hummingbird-logo.svg
  </files>
  <action>
Create mobile-first navigation component following WCAG 2.5.5 touch target guidelines (44x44px minimum).

1. Create directory: src/components/navigation/

2. Create src/components/navigation/Navigation.tsx:
   ```typescript
   'use client'

   import { useState } from 'react'
   import Link from 'next/link'
   import Image from 'next/image'

   const navItems = [
     { name: 'Shop', href: '/shop' },
     { name: 'Our Story', href: '/our-story' },
     { name: 'Recipes', href: '/recipes' },
     { name: 'Subscribe', href: '/subscribe', comingSoon: true },
     { name: 'Cart', href: '/cart' },
   ]

   export default function Navigation() {
     const [mobileMenuOpen, setMobileMenuOpen] = useState(false)

     return (
       <nav className="bg-brand-dark border-b border-brand-gold/20">
         <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
           <div className="flex justify-between items-center h-16">
             {/* Logo */}
             <Link href="/" className="flex items-center space-x-2">
               <Image
                 src="/images/hummingbird-logo.svg"
                 alt="Jamaica House Brand"
                 width={40}
                 height={40}
                 priority
               />
               <span className="text-white font-bold text-lg hidden sm:inline">
                 Jamaica House
               </span>
             </Link>

             {/* Desktop navigation */}
             <div className="hidden md:flex space-x-8">
               {navItems.map((item) => (
                 <Link
                   key={item.name}
                   href={item.href}
                   className="text-white hover:text-brand-gold transition-colors text-sm font-medium"
                 >
                   {item.name}
                   {item.comingSoon && (
                     <span className="ml-1 text-xs text-brand-gold opacity-75">
                       (Soon)
                     </span>
                   )}
                 </Link>
               ))}
             </div>

             {/* Mobile menu button - MUST be 44x44px minimum */}
             <button
               type="button"
               onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
               aria-expanded={mobileMenuOpen}
               aria-label="Toggle navigation menu"
               className="md:hidden w-11 h-11 flex items-center justify-center text-white hover:text-brand-gold transition-colors"
             >
               <span className="sr-only">
                 {mobileMenuOpen ? 'Close menu' : 'Open menu'}
               </span>
               {/* Hamburger icon */}
               <svg
                 className="w-6 h-6"
                 fill="none"
                 viewBox="0 0 24 24"
                 stroke="currentColor"
                 strokeWidth={2}
               >
                 {mobileMenuOpen ? (
                   <path
                     strokeLinecap="round"
                     strokeLinejoin="round"
                     d="M6 18L18 6M6 6l12 12"
                   />
                 ) : (
                   <path
                     strokeLinecap="round"
                     strokeLinejoin="round"
                     d="M4 6h16M4 12h16M4 18h16"
                   />
                 )}
               </svg>
             </button>
           </div>

           {/* Mobile menu panel */}
           {mobileMenuOpen && (
             <div className="md:hidden pb-4 border-t border-brand-gold/10 mt-2">
               {navItems.map((item) => (
                 <Link
                   key={item.name}
                   href={item.href}
                   className="block py-3 text-white hover:text-brand-gold transition-colors text-base"
                   onClick={() => setMobileMenuOpen(false)}
                 >
                   {item.name}
                   {item.comingSoon && (
                     <span className="ml-2 text-xs text-brand-gold opacity-75">
                       (Coming Soon)
                     </span>
                   )}
                 </Link>
               ))}
             </div>
           )}
         </div>
       </nav>
     )
   }
   ```

3. Create placeholder logo at public/images/hummingbird-logo.svg (temporary simple SVG):
   ```svg
   <svg width="40" height="40" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
     <circle cx="20" cy="20" r="18" fill="#D4A843"/>
     <text x="20" y="26" text-anchor="middle" font-size="16" font-weight="bold" fill="#1A1A1A">JH</text>
   </svg>
   ```

Why 'use client': Navigation uses useState for mobile menu toggle, requires client-side interactivity.

Why aria-expanded: Tells screen readers whether menu is open/closed (WCAG 4.1.2).

Why w-11 h-11: 44px minimum touch target for WCAG 2.5.5 compliance (11 * 4px = 44px).

Why separate mobile/desktop: Mobile-first responsive design - hamburger under md breakpoint, horizontal above.

Do NOT use aria-label changes for open/closed state - use aria-expanded. Do NOT make hamburger button smaller than 44px. Do NOT forget sr-only text for screen readers.
  </action>
  <verify>
```bash
cd /Users/rfmstaff/Desktop/jamaica-house-brand
npm run dev
```
Visit http://localhost:3000 on desktop - see horizontal nav with 5 items. Resize to mobile - see hamburger menu. Click hamburger - menu opens/closes. Inspect button - verify w-11 h-11 classes (44px). Check aria-expanded attribute toggles true/false.
  </verify>
  <done>
Navigation component exists with logo, 5 menu items, mobile hamburger menu, desktop horizontal layout, 44px+ touch targets, proper ARIA attributes (aria-expanded, aria-label, sr-only). Component is marked 'use client' and uses useState.
  </done>
</task>

<task type="auto">
  <name>Integrate navigation into root layout</name>
  <files>
src/app/layout.tsx
src/app/page.tsx
  </files>
  <action>
Add Navigation component to root layout so it appears on all pages.

1. Update src/app/layout.tsx to import and render Navigation:
   ```typescript
   import type { Metadata } from "next";
   import { plusJakarta } from "@/lib/fonts";
   import Navigation from "@/components/navigation/Navigation";
   import "./globals.css";

   export const metadata: Metadata = {
     title: "Jamaica House Brand - Original Jerk Sauce",
     description: "Authentic Jamaican jerk sauce with 30+ years of restaurant heritage",
   };

   export default function RootLayout({
     children,
   }: Readonly<{
     children: React.ReactNode;
   }>) {
     return (
       <html lang="en" className={`${plusJakarta.variable} dark`}>
         <body className="bg-brand-dark text-white antialiased">
           <Navigation />
           {children}
         </body>
       </html>
     );
   }
   ```

2. Update src/app/page.tsx to add top padding (navigation is 4rem tall):
   ```typescript
   export default function Home() {
     return (
       <main className="min-h-screen pt-16 flex items-center justify-center">
         <div className="text-center px-4">
           <h1 className="text-5xl md:text-6xl font-bold text-brand-gold mb-6">
             Jamaica House Brand
           </h1>
           <p className="text-xl md:text-2xl text-white mb-4">
             30 Years of Flavor. One Legendary Sauce.
           </p>
           <p className="text-brand-gold/80 text-sm uppercase tracking-wide">
             From Our Family to Yours
           </p>
         </div>
       </main>
     );
   }
   ```

Do NOT wrap Navigation in a <header> tag - Navigation component already uses semantic <nav>. Do NOT add additional spacing/margins around Navigation beyond what's in the component. Layout should be minimal - just Navigation + children.
  </action>
  <verify>
```bash
cd /Users/rfmstaff/Desktop/jamaica-house-brand
npm run dev
```
Visit http://localhost:3000 - navigation appears at top with logo and menu items. Content below navigation has proper spacing (pt-16 on main). Resize browser - navigation responds correctly on mobile and desktop.
  </verify>
  <done>
Navigation component is integrated into root layout. Appears on all pages. Homepage content has proper spacing below navigation. Navigation links are visible and styled correctly (white text, gold hover). Mobile menu works. Logo displays.
  </done>
</task>

</tasks>

<verification>
**Navigation functionality:**
1. Desktop view (≥768px): Horizontal nav with 5 items visible
2. Mobile view (<768px): Hamburger menu button appears, menu items hidden
3. Click hamburger: Menu slides down, X icon appears
4. Click menu item: Menu closes, would navigate to page
5. Click logo: Would navigate to home page

**Accessibility:**
1. Inspect hamburger button: aria-expanded="false" when closed, "true" when open
2. Inspect hamburger button: aria-label="Toggle navigation menu"
3. Screen reader test: sr-only text announces "Open menu" / "Close menu"
4. Measure button: 44px x 44px minimum (w-11 h-11)
5. Tab through navigation: Focus visible on all links

**Visual:**
1. Logo displays (placeholder SVG circle with "JH")
2. Subscribe link shows "(Soon)" suffix in gold
3. Hover states work: text-white → text-brand-gold transition
4. Border appears below nav: border-brand-gold/20
</verification>

<success_criteria>
- Navigation component renders with logo and 5 menu items
- Mobile hamburger menu toggles open/closed with proper animation
- Desktop horizontal menu displays above md breakpoint
- Touch targets meet WCAG 2.5.5 (44px minimum)
- ARIA attributes present: aria-expanded, aria-label, sr-only
- Navigation integrated into root layout, appears on all pages
- Hover states work with brand-gold color
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
