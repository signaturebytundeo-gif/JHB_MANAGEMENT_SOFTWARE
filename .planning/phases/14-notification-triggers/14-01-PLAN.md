---
phase: 14-notification-triggers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/command-center/src/lib/email-templates/DeliveryConfirmation.tsx
  - apps/command-center/src/lib/emails/customer-emails.ts
  - apps/command-center/prisma/schema.prisma
  - apps/command-center/src/lib/integrations/slack.ts
autonomous: true

must_haves:
  truths:
    - "A DeliveryConfirmation React Email template exists with JHB branding (BaseLayout wrapper)"
    - "sendDeliveryConfirmationEmail function exists and can render + send via Resend"
    - "deliveryEmailSentAt field exists on WebsiteOrder model in Prisma schema"
    - "Slack notification function exists for delivery email events"
  artifacts:
    - path: "apps/command-center/src/lib/email-templates/DeliveryConfirmation.tsx"
      provides: "Branded delivery confirmation React Email component"
      contains: "BaseLayout"
    - path: "apps/command-center/src/lib/emails/customer-emails.ts"
      provides: "sendDeliveryConfirmationEmail function"
      exports: ["sendOrderConfirmationEmail", "sendShippingConfirmationEmail", "sendDeliveryConfirmationEmail"]
    - path: "apps/command-center/prisma/schema.prisma"
      provides: "deliveryEmailSentAt field on WebsiteOrder"
      contains: "deliveryEmailSentAt"
    - path: "apps/command-center/src/lib/integrations/slack.ts"
      provides: "notifyDeliveryEmailSent function"
      exports: ["notifyShippingEmailSent", "notifyOrderEmailFailed", "notifyDeliveryEmailSent"]
  key_links:
    - from: "apps/command-center/src/lib/emails/customer-emails.ts"
      to: "apps/command-center/src/lib/email-templates/DeliveryConfirmation.tsx"
      via: "import and React.createElement"
      pattern: "DeliveryConfirmationEmail"
---

<objective>
Create the delivery confirmation email template, send function, database field, and Slack notification for the DELIVERED lifecycle event.

Purpose: The order confirmation (NOTIF-01) and shipping confirmation (NOTIF-02) triggers are already wired from Phases 12-13. This plan builds the missing artifacts for NOTIF-03 (delivery confirmation).
Output: DeliveryConfirmation.tsx template, sendDeliveryConfirmationEmail function, deliveryEmailSentAt schema field, notifyDeliveryEmailSent Slack function.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 12 summaries — established email patterns
@/Users/rfmstaff/.planning/phases/12-email-foundation/12-02-SUMMARY.md

# Existing email infrastructure to follow patterns from
@apps/command-center/src/lib/email-templates/BaseLayout.tsx
@apps/command-center/src/lib/email-templates/ShippingConfirmation.tsx
@apps/command-center/src/lib/emails/customer-emails.ts
@apps/command-center/src/lib/integrations/slack.ts
@apps/command-center/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeliveryConfirmation email template and send function</name>
  <files>
    apps/command-center/src/lib/email-templates/DeliveryConfirmation.tsx
    apps/command-center/src/lib/emails/customer-emails.ts
  </files>
  <action>
1. Create `DeliveryConfirmation.tsx` in `src/lib/email-templates/` following the exact same pattern as `ShippingConfirmation.tsx`:
   - Import and wrap with `BaseLayout` (dark header, gold monogram, cream body, green footer)
   - Props interface: `{ customerFirstName: string; orderId: string; carrier?: string; trackingNumber?: string }`
   - Content: Congratulatory message that their order has been delivered, order ID reference
   - If carrier + trackingNumber provided, include a "View Delivery Details" button linking to tracking URL (reuse the `getTrackingUrl` pattern from ShippingConfirmation.tsx — copy the helper into this file or import it)
   - Include a "Need help?" section with a link to orders@jamaicahousebrand.com
   - Subject line: "Your Jamaica House Brand Order Has Been Delivered!"
   - Use inline styles (no className/Tailwind) — same pattern as other templates
   - Export as named export: `DeliveryConfirmationEmail`

2. Add `sendDeliveryConfirmationEmail` to `customer-emails.ts`:
   - Follow exact pattern of `sendShippingConfirmationEmail`
   - Import `DeliveryConfirmationEmail` from `../email-templates/DeliveryConfirmation`
   - Interface `DeliveryConfirmationData`: `{ customerFirstName, customerEmail, orderId, carrier?, trackingNumber? }`
   - Dev fallback console.log block (same pattern as existing functions)
   - Use `render(React.createElement(DeliveryConfirmationEmail, {...}))` — NOT renderAsync (per Phase 12 decision)
   - Send from `FROM_ADDRESS` (already defined as `Jamaica House Brand <orders@jamaicahousebrand.com>`)
   - Subject: "Your Jamaica House Brand Order Has Been Delivered!"
   - Return `{ success: boolean; error?: string }`
  </action>
  <verify>
Run `cd apps/command-center && npx tsc --noEmit` — 0 TypeScript errors. Confirm DeliveryConfirmation.tsx exists and exports DeliveryConfirmationEmail. Confirm customer-emails.ts exports sendDeliveryConfirmationEmail.
  </verify>
  <done>DeliveryConfirmation.tsx template exists with BaseLayout wrapper and JHB branding. sendDeliveryConfirmationEmail function exists in customer-emails.ts with dev fallback, Resend send, and proper error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Add deliveryEmailSentAt schema field and Slack notification function</name>
  <files>
    apps/command-center/prisma/schema.prisma
    apps/command-center/src/lib/integrations/slack.ts
  </files>
  <action>
1. Add `deliveryEmailSentAt DateTime?` to the `WebsiteOrder` model in `prisma/schema.prisma`, directly after the existing `shippingEmailSentAt DateTime?` line. Then run `cd apps/command-center && npx prisma db push` to apply the schema change (this project uses db push, not migrate).

2. Add `notifyDeliveryEmailSent` to `slack.ts` following the `notifyShippingEmailSent` pattern:
   - Interface `DeliveryEmailData`: `{ customerName, customerEmail, orderId, emailSuccess, errorMessage? }`
   - Header: "Order Delivered - Email Sent" or "Order Delivered - EMAIL FAILED" based on emailSuccess
   - Fields: Customer, Email, Order ID, Email Status (with emoji), Timestamp (America/New_York)
   - Same try/catch pattern as existing functions
   - Export the function
  </action>
  <verify>
Run `cd apps/command-center && npx prisma db push` succeeds. Run `npx tsc --noEmit` — 0 TypeScript errors. Confirm `deliveryEmailSentAt` appears in schema. Confirm `notifyDeliveryEmailSent` is exported from slack.ts.
  </verify>
  <done>deliveryEmailSentAt field exists on WebsiteOrder model and is synced to database. notifyDeliveryEmailSent Slack function exists and follows the same pattern as notifyShippingEmailSent.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors in apps/command-center
- DeliveryConfirmation.tsx follows BaseLayout pattern (grep for BaseLayout import)
- customer-emails.ts exports all three send functions
- schema.prisma has deliveryEmailSentAt on WebsiteOrder
- slack.ts exports notifyDeliveryEmailSent
</verification>

<success_criteria>
All delivery email artifacts exist and compile: template component, send function, DB tracking field, Slack notification. Ready for Plan 14-02 to wire into the order status change flow.
</success_criteria>

<output>
After completion, create `.planning/phases/14-notification-triggers/14-01-SUMMARY.md`
</output>
