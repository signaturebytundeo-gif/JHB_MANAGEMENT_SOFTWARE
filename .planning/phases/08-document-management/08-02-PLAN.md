---
phase: 08-document-management
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/command-center/src/app/(dashboard)/dashboard/documents/page.tsx
  - apps/command-center/src/app/(dashboard)/dashboard/documents/[id]/page.tsx
  - apps/command-center/src/components/documents/DocumentUploadForm.tsx
  - apps/command-center/src/components/documents/DocumentList.tsx
  - apps/command-center/src/components/documents/DocumentVersionHistory.tsx
  - apps/command-center/src/components/layout/Sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "User can see a list of uploaded documents with category, linked record, and upload date"
    - "User can upload a new document with name, category, and optional record link"
    - "User can view version history for any document on its detail page"
    - "User can upload a new version of an existing document"
    - "User can download any document version via its blob URL"
    - "Documents page is accessible from the sidebar navigation"
  artifacts:
    - path: "apps/command-center/src/app/(dashboard)/dashboard/documents/page.tsx"
      provides: "Documents list page with upload form"
      min_lines: 30
    - path: "apps/command-center/src/app/(dashboard)/dashboard/documents/[id]/page.tsx"
      provides: "Document detail page with version history"
      min_lines: 30
    - path: "apps/command-center/src/components/documents/DocumentUploadForm.tsx"
      provides: "Client upload form with file input, category select, record linking"
      min_lines: 40
    - path: "apps/command-center/src/components/documents/DocumentList.tsx"
      provides: "Filterable document table/cards"
      min_lines: 30
    - path: "apps/command-center/src/components/documents/DocumentVersionHistory.tsx"
      provides: "Version history table with download links"
      min_lines: 20
  key_links:
    - from: "apps/command-center/src/components/documents/DocumentUploadForm.tsx"
      to: "apps/command-center/src/app/actions/documents.ts"
      via: "useActionState with uploadDocument action"
      pattern: "useActionState.*uploadDocument"
    - from: "apps/command-center/src/app/(dashboard)/dashboard/documents/page.tsx"
      to: "apps/command-center/src/app/actions/documents.ts"
      via: "getDocuments server call"
      pattern: "getDocuments"
    - from: "apps/command-center/src/components/layout/Sidebar.tsx"
      to: "/dashboard/documents"
      via: "navItems entry with FileText icon"
      pattern: "documents"
---

<objective>
Build the Document Management UI: documents list page with filtering, upload form with category and record linking, document detail page with version history, and sidebar navigation entry.

Purpose: Users need to upload, browse, and manage documents linked to their business records. This delivers DOC-01 (upload with categories), DOC-02 (record linking), and DOC-03 (version history).
Output: Fully functional document management pages accessible from sidebar.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-document-management/08-RESEARCH.md
@.planning/phases/08-document-management/08-01-SUMMARY.md
@apps/command-center/src/app/actions/documents.ts
@apps/command-center/src/lib/validators/documents.ts
@apps/command-center/src/components/layout/Sidebar.tsx
@apps/command-center/src/app/(dashboard)/dashboard/orders/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documents list page, upload form, and document list component</name>
  <files>
    apps/command-center/src/app/(dashboard)/dashboard/documents/page.tsx
    apps/command-center/src/components/documents/DocumentUploadForm.tsx
    apps/command-center/src/components/documents/DocumentList.tsx
    apps/command-center/src/components/layout/Sidebar.tsx
  </files>
  <action>
1. **Sidebar.tsx** — Add Documents nav item BEFORE Settings in the navItems array:
   `{ label: 'Documents', href: '/dashboard/documents', icon: FileText, permission: 'documents' }`
   Add `FileText` to the lucide-react import.

2. **DocumentUploadForm.tsx** — Client component ('use client') using useActionState with the uploadDocument server action:
   - File input (accept common document types: .pdf,.doc,.docx,.xls,.xlsx,.csv,.jpg,.jpeg,.png,.txt)
   - Name text input (required)
   - Category select dropdown: Agreement, Invoice, Certification, SOP, Marketing, Other
   - "Link to" select: None, Customer, Order, Batch
   - When a link type is selected, show a second select populated with records. Fetch customers/orders/batches via existing server actions (getCRMCustomers from crm-customers.ts, getOperatorOrders from operator-orders.ts, getBatches from production.ts). Pass these as props from the server page.
   - linkedId hidden input set by the record select
   - Submit button with pending state (useFormStatus or check state)
   - Show success/error toast via sonner on state change
   - On success, reset form
   - Follow the pattern used in expense upload forms

3. **DocumentList.tsx** — Client component for displaying documents:
   - Receive documents array as prop (typed from getDocuments return)
   - Category filter tabs or select at top (All, Agreement, Invoice, etc.)
   - Client-side filtering (small dataset — same pattern as batch filtering)
   - Table layout on desktop, card layout on mobile (responsive table-to-cards pattern from production)
   - Each row shows: name, category badge, linked record (customer name / order number / batch code or "Unlinked"), version count, uploaded by, date
   - Name links to /dashboard/documents/[id] detail page
   - Download button linking to currentBlobUrl (target="_blank")
   - Delete button that calls deleteDocument action with confirmation

4. **documents/page.tsx** — Server component:
   - Call getDocuments() to fetch all documents
   - Call getCRMCustomers(), getOperatorOrders() (or relevant list action), getBatches() for the link dropdowns — pass as props to DocumentUploadForm
   - Render page header "Documents" with upload form (in a collapsible Card or Dialog)
   - Render DocumentList with fetched documents
   - Follow the layout pattern of other dashboard pages (e.g., orders/page.tsx)
  </action>
  <verify>
Navigate to /dashboard/documents in the browser — page renders without errors. Upload form shows all fields. Document list displays any existing documents. Sidebar shows "Documents" link with FileText icon. Category filter works client-side.
  </verify>
  <done>
Documents list page renders with category filtering, upload form accepts file + metadata + optional record link, sidebar has Documents entry, responsive layout works on mobile and desktop.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document detail page with version history and new version upload</name>
  <files>
    apps/command-center/src/app/(dashboard)/dashboard/documents/[id]/page.tsx
    apps/command-center/src/components/documents/DocumentVersionHistory.tsx
  </files>
  <action>
1. **DocumentVersionHistory.tsx** — Client component showing version history table:
   - Receive versions array as prop (from getDocumentById include)
   - Table with columns: Version #, File Name, Size (formatted KB/MB), Uploaded By, Date, Download
   - Each row has a download link (anchor tag with href={version.blobUrl} target="_blank" download)
   - Current/latest version highlighted with a badge
   - Format file size using simple helper: bytes < 1024 = bytes + ' B', < 1MB = (bytes/1024).toFixed(1) + ' KB', else (bytes/1048576).toFixed(1) + ' MB'
   - Format dates with date-fns format()

2. **Upload New Version form** — Either inline in the detail page or as a small component:
   - File input only (name and category inherited from parent document)
   - Uses useActionState with uploadNewVersion action
   - Hidden input for documentId
   - Show toast on success/error
   - On success, the page revalidates via revalidatePath (server action handles this)

3. **documents/[id]/page.tsx** — Server component:
   - Call getDocumentById(params.id)
   - If not found, call notFound()
   - Display document metadata: name, category badge, linked record (with link to the record's page if applicable), uploaded by, created date
   - Show current version download button prominently
   - Render DocumentVersionHistory with document.versions
   - Render "Upload New Version" form below version history
   - Back link to /dashboard/documents
   - Follow the detail page pattern from /dashboard/orders/[id] or /dashboard/production/[id]
  </action>
  <verify>
Navigate to /dashboard/documents/[id] for an existing document — page renders with document info, version history table, and new version upload form. Download links work (open blob URL in new tab). Upload new version creates a new version row.
  </verify>
  <done>
Document detail page shows metadata, version history with download links for each version, and a form to upload new versions. Version numbers increment correctly. File sizes display formatted. Dates formatted with date-fns.
  </done>
</task>

</tasks>

<verification>
1. /dashboard/documents page loads without errors
2. Documents sidebar link visible for ADMIN and MANAGER roles
3. Upload form submits and creates document in DB
4. Document list shows uploaded documents with filtering
5. Clicking document name navigates to detail page
6. Detail page shows version history
7. "Upload New Version" creates new version entry
8. Download links open blob URLs
</verification>

<success_criteria>
- DOC-01: User can upload documents with categories (Agreements, Invoices, Certifications, SOPs, Marketing)
- DOC-02: Documents can be linked to customers, orders, or batches
- DOC-03: System tracks document versions and shows version history
- Documents page accessible via sidebar
- Responsive layout (table on desktop, cards on mobile)
</success_criteria>

<output>
After completion, create `.planning/phases/08-document-management/08-02-SUMMARY.md`
</output>
