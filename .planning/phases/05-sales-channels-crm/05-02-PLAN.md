---
phase: 05-sales-channels-crm
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/command-center/src/app/actions/crm-customers.ts
  - apps/command-center/src/app/(dashboard)/dashboard/customers/page.tsx
  - apps/command-center/src/app/(dashboard)/dashboard/customers/client.tsx
  - apps/command-center/src/app/(dashboard)/dashboard/customers/[id]/page.tsx
  - apps/command-center/src/components/crm/CustomerForm.tsx
  - apps/command-center/src/components/crm/CustomerDetail.tsx
  - apps/command-center/src/components/crm/CRMTabs.tsx
autonomous: true

must_haves:
  truths:
    - "User can create a new customer with type, contact info, payment terms, and addresses"
    - "User can view customer detail page with profile info and purchase history"
    - "User can see customer lifetime value calculated from order totals"
    - "Customer list page has tabbed navigation for CRM sections"
    - "User can edit existing customer profile"
  artifacts:
    - path: "apps/command-center/src/app/actions/crm-customers.ts"
      provides: "Customer CRUD server actions"
      exports: ["createCRMCustomer", "updateCRMCustomer", "getCRMCustomerById", "getCRMCustomers"]
    - path: "apps/command-center/src/app/(dashboard)/dashboard/customers/[id]/page.tsx"
      provides: "Customer detail page with purchase history"
      min_lines: 30
    - path: "apps/command-center/src/components/crm/CustomerForm.tsx"
      provides: "Create/edit customer form with useActionState"
      min_lines: 50
    - path: "apps/command-center/src/components/crm/CRMTabs.tsx"
      provides: "Tabbed navigation for CRM sections"
      min_lines: 20
  key_links:
    - from: "apps/command-center/src/components/crm/CustomerForm.tsx"
      to: "apps/command-center/src/app/actions/crm-customers.ts"
      via: "useActionState form submission"
      pattern: "useActionState.*createCRMCustomer"
    - from: "apps/command-center/src/app/(dashboard)/dashboard/customers/[id]/page.tsx"
      to: "apps/command-center/src/app/actions/crm-customers.ts"
      via: "server component data fetch"
      pattern: "getCRMCustomerById"
---

<objective>
Build the customer CRM hub: expanded customer profiles with type/payment terms/addresses, customer detail page with purchase history and lifetime value, and tabbed CRM navigation.

Purpose: Satisfies CRM-01 (customer profiles with contact info, payment terms, credit limit), CRM-02 (customer types), and CRM-03 (purchase history and lifetime value).
Output: Full customer CRUD with detail pages, tabbed CRM layout that Plan 03 and 04 will plug into.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sales-channels-crm/05-RESEARCH.md
@.planning/phases/05-sales-channels-crm/05-01-SUMMARY.md
@apps/command-center/src/app/actions/customers.ts
@apps/command-center/src/app/(dashboard)/dashboard/customers/page.tsx
@apps/command-center/src/app/(dashboard)/dashboard/customers/client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CRM customer server actions and detail page</name>
  <files>
    apps/command-center/src/app/actions/crm-customers.ts
    apps/command-center/src/app/(dashboard)/dashboard/customers/[id]/page.tsx
    apps/command-center/src/components/crm/CustomerDetail.tsx
  </files>
  <action>
1. Create crm-customers.ts (DO NOT modify existing customers.ts — it serves the website customer view):
   - Import db from lib/db, revalidatePath, and validators from lib/validators/crm-customers.ts
   - getCRMCustomers(): findMany with select including new fields (customerType, company, paymentTerms, creditLimit), include orderCount from _count of operatorOrders relation, sort by createdAt desc
   - getCRMCustomerById(id: string): findUnique including ALL new fields, include operatorOrders with lineItems and product (for purchase history), include distributorAgreements and subscriptionMembers with plan
   - createCRMCustomer(prevState: FormState, formData: FormData): validate with createCustomerSchema, db.customer.create with all CRM fields, revalidatePath('/dashboard/customers'), return { success: true }
   - updateCRMCustomer(prevState: FormState, formData: FormData): validate with updateCustomerSchema, extract id from formData, db.customer.update, revalidatePath, return { success: true }
   - getCustomerPurchaseHistory(customerId: string): query Order where customerId, include lineItems with product, orderBy createdAt desc. Calculate lifetime value as sum of all order totals.

2. Create CustomerDetail.tsx client component:
   - Receives customer prop with full relations (orders, distributor agreements, subscriptions)
   - Profile section: display customer type badge, contact info, company, payment terms, credit limit, addresses
   - Purchase History section: table of orders (date, order number, channel, total, status) sorted by date desc
   - Lifetime Value card: total spent across all orders, order count, average order value
   - Edit button that opens CustomerForm in edit mode (modal overlay using fixed inset-0 pattern)

3. Create [id]/page.tsx server component:
   - Import getCRMCustomerById
   - Fetch customer with full relations
   - If not found, call notFound()
   - Render CustomerDetail component with customer data
   - Include breadcrumb: Customers > [customer name]
  </action>
  <verify>
    cd apps/command-center && npx tsc --noEmit 2>&1 | grep -E "(crm-customers|CustomerDetail|\[id\]/page)" || echo "No errors in CRM customer files"
  </verify>
  <done>
    getCRMCustomerById returns customer with all CRM fields + purchase history. Customer detail page renders profile, purchase history table, and lifetime value. Edit modal opens with existing data pre-filled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CustomerForm and CRM tabbed navigation</name>
  <files>
    apps/command-center/src/components/crm/CustomerForm.tsx
    apps/command-center/src/components/crm/CRMTabs.tsx
    apps/command-center/src/app/(dashboard)/dashboard/customers/page.tsx
    apps/command-center/src/app/(dashboard)/dashboard/customers/client.tsx
  </files>
  <action>
1. Create CustomerForm.tsx ('use client'):
   - Props: { customer?: Customer; onClose?: () => void } (undefined = create mode, defined = edit mode)
   - Use useActionState with createCRMCustomer or updateCRMCustomer based on mode
   - Fields: firstName, lastName, email, phone, customerType (Select dropdown with CustomerType values), company (optional), paymentTerms (Select: net_30, net_15, cash), creditLimit (number input), billingAddress, shippingAddress, city, state, zip, notes (textarea)
   - In edit mode, include hidden input for id
   - useEffect for toast.success on state.success, call onClose if modal
   - SubmitButton with useFormStatus pending state
   - When used as modal: wrap in fixed inset-0 bg-black/50 z-50 overlay (per established pattern)
   - When used as standalone page: no overlay wrapper

2. Create CRMTabs.tsx ('use client'):
   - Props: { profiles: ReactNode; subscriptions: ReactNode; distributors: ReactNode; leads: ReactNode }
   - useState for activeTab: 'profiles' | 'subscriptions' | 'distributors' | 'leads'
   - Render tab bar with border-bottom indicator following the established OrdersTabs pattern
   - Tab labels: "Customer Profiles", "Subscriptions", "Distributors", "Leads"
   - Conditionally render the matching ReactNode based on activeTab
   - Use caribbean-green for active tab border color (matches existing theme)

3. Update customers/page.tsx (server component):
   - Import CRMTabs, getCRMCustomers
   - Fetch CRM customer list
   - Render CRMTabs with profiles slot populated with updated client.tsx
   - Other slots (subscriptions, distributors, leads) render placeholder text: "Coming soon" — Plans 03 and 04 will replace these
   - Add "New Customer" button that links to inline form or triggers modal

4. Update customers/client.tsx:
   - Extend CustomerRow type to include new CRM fields (customerType, company, paymentTerms, creditLimit)
   - Add customerType badge column to table
   - Add "New Customer" button that opens CustomerForm modal
   - Keep existing search/filter functionality
   - Make customer name clickable, linking to /dashboard/customers/[id]
  </action>
  <verify>
    cd apps/command-center && npx tsc --noEmit 2>&1 | grep -E "(CustomerForm|CRMTabs|customers)" || echo "No errors in CRM UI files"
  </verify>
  <done>
    CustomerForm renders create/edit modes with all CRM fields, submits via useActionState. CRMTabs shows 4 tab navigation with profiles active by default. Customer list shows type badges and links to detail pages. "New Customer" button opens modal form.
  </done>
</task>

</tasks>

<verification>
1. Navigate to /dashboard/customers — see CRM tabs with 4 sections
2. Click "New Customer" — modal form appears with all CRM fields
3. Submit form — customer created with type, payment terms, credit limit
4. Click customer name — detail page shows profile + purchase history + LTV
5. Click edit on detail page — form pre-filled with existing data
6. TypeScript compiles with no errors
</verification>

<success_criteria>
- Customer CRUD operations work: create, read detail, update, list
- Customer detail page shows purchase history from linked orders
- Lifetime value calculated as sum of order totals
- CRM tabs navigation renders 4 sections (only profiles active, others placeholder)
- Customer type badge visible in list view
- All forms use useActionState pattern (not React Hook Form)
- Modal overlays use fixed inset-0 pattern (not Radix Dialog)
</success_criteria>

<output>
After completion, create `.planning/phases/05-sales-channels-crm/05-02-SUMMARY.md`
</output>
