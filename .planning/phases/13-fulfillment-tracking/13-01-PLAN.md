---
phase: 13-fulfillment-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/command-center/src/components/orders/FulfillmentModal.tsx
  - apps/command-center/src/app/actions/orders.ts
  - apps/command-center/src/app/(dashboard)/dashboard/orders/[id]/page.tsx
autonomous: false

must_haves:
  truths:
    - "FulfillmentModal shows a tracking number input field"
    - "Submitting the modal with a tracking number saves it to the WebsiteOrder record"
    - "The order detail page displays the tracking number after it has been entered"
    - "Marking shipped without a tracking number still works (tracking is optional)"
  artifacts:
    - path: "apps/command-center/src/components/orders/FulfillmentModal.tsx"
      provides: "Ship Order modal with optional tracking number + carrier fields"
      contains: "trackingNumber"
    - path: "apps/command-center/src/app/actions/orders.ts"
      provides: "fulfillOrder server action that persists tracking data"
      contains: "fulfillOrder"
    - path: "apps/command-center/src/app/(dashboard)/dashboard/orders/[id]/page.tsx"
      provides: "Order detail page displaying tracking number and carrier"
      contains: "trackingNumber"
  key_links:
    - from: "FulfillmentModal.tsx"
      to: "orders.ts fulfillOrder action"
      via: "form submit -> fulfillOrder(orderId, { trackingNumber?, carrier? })"
      pattern: "fulfillOrder\\(orderId"
    - from: "orders.ts fulfillOrder"
      to: "prisma.websiteOrder.update"
      via: "database write of trackingNumber, carrier, shippedAt"
      pattern: "trackingNumber.*carrier"
    - from: "orders/[id]/page.tsx"
      to: "orders.ts getWebsiteOrderById"
      via: "server component reads order and renders tracking section"
      pattern: "order\\.trackingNumber"
---

<objective>
Make tracking number optional in the FulfillmentModal and ensure end-to-end fulfillment tracking works.

Purpose: The FulfillmentModal currently requires both carrier and tracking number to submit. The phase requirement states tracking number must be optional -- operators should be able to mark an order as shipped without entering a tracking number. The database schema already has nullable trackingNumber/carrier fields, and the order detail page already displays tracking when present. The only changes needed are in the FulfillmentModal validation logic and the fulfillOrder server action guard.

Output: FulfillmentModal allows shipping with or without tracking info; order detail page displays tracking when present; all four success criteria pass.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/command-center/src/components/orders/FulfillmentModal.tsx
@apps/command-center/src/app/actions/orders.ts
@apps/command-center/src/app/(dashboard)/dashboard/orders/[id]/page.tsx
@apps/command-center/src/components/orders/OrderStatusControls.tsx
@apps/command-center/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make tracking number and carrier optional in FulfillmentModal + fulfillOrder action</name>
  <files>
    apps/command-center/src/components/orders/FulfillmentModal.tsx
    apps/command-center/src/app/actions/orders.ts
  </files>
  <action>
In FulfillmentModal.tsx:

1. Remove the validation guard that blocks submission without carrier/tracking. Currently line ~41: `if (!carrier || !trackingNumber.trim()) { toast.error(...); return; }` -- delete this entire block. The form should submit even when carrier and tracking are empty.

2. Remove `required` attribute from the tracking number Input (currently line ~110).

3. Update the submit button disabled condition (currently line ~128): change `disabled={isSubmitting || !carrier || !trackingNumber.trim()}` to just `disabled={isSubmitting}`.

4. Update the call to fulfillOrder (lines ~49-53) to pass trackingNumber and carrier only when non-empty:
   ```
   trackingNumber: trackingNumber.trim() || undefined,
   carrier: carrier || undefined,
   ```

5. Update the button label to be more generic when no tracking is provided. Change the button text from always saying "Ship & Notify Customer" to something like: `{isSubmitting ? 'Shipping...' : (trackingNumber.trim() ? 'Ship & Notify Customer' : 'Mark as Shipped')}` -- this way the operator knows that without tracking info, the email may not include tracking details.

6. Update the description text (line ~83-85) to indicate tracking is optional: "Shipping order for {customerName}. Add tracking info below (optional) -- the customer will receive a shipping confirmation email."

In orders.ts (fulfillOrder action):

7. Update the `data` parameter type to make trackingNumber and carrier optional: `data: { trackingNumber?: string; carrier?: string; estimatedDelivery?: string }`.

8. In the db.websiteOrder.update call (lines ~190-198), only set trackingNumber and carrier if they are provided (truthy). This prevents overwriting existing values with undefined:
   ```
   data: {
     status: 'SHIPPED',
     ...(data.trackingNumber && { trackingNumber: data.trackingNumber }),
     ...(data.carrier && { carrier: data.carrier }),
     shippedAt: new Date(),
   },
   ```

9. In the sendShippingConfirmationEmail call (lines ~205-212), still pass trackingNumber and carrier even if undefined -- the email template should handle missing tracking gracefully (it already does since these are optional params).
  </action>
  <verify>
Run `cd apps/command-center && npx tsc --noEmit` to confirm no TypeScript errors. Visually inspect the FulfillmentModal code to confirm: (a) no `required` on tracking input, (b) submit button enabled without tracking, (c) fulfillOrder type accepts optional fields.
  </verify>
  <done>
FulfillmentModal can be submitted without a tracking number or carrier. When tracking info IS provided, it is saved to the database. The fulfillOrder server action accepts optional tracking fields.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify fulfillment flow end-to-end</name>
  <what-built>
Made tracking number and carrier optional in the FulfillmentModal. The order detail page already displays tracking info when present (this was built in a prior phase). No Prisma migration was needed -- the schema already has nullable trackingNumber and carrier fields on WebsiteOrder.
  </what-built>
  <how-to-verify>
1. Run `cd apps/command-center && npm run dev`
2. Navigate to /dashboard/orders and find a PROCESSING order (or advance a NEW order to PROCESSING first)
3. Click "Mark as Shipped" to open the FulfillmentModal
4. TEST A -- Ship WITHOUT tracking: Leave carrier and tracking number blank. Click "Mark as Shipped". Confirm the order advances to SHIPPED status.
5. Go to the order detail page and confirm it does NOT show a tracking section (since no tracking was entered).
6. TEST B -- Ship WITH tracking: Find another PROCESSING order. Open the FulfillmentModal. Select "UPS" as carrier, enter "1Z999AA10123456784" as tracking number. Click "Ship & Notify Customer". Confirm the order advances to SHIPPED.
7. Go to the order detail page and confirm the Tracking section appears showing carrier "UPS" and tracking number "1Z999AA10123456784".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- FulfillmentModal renders without required tracking fields
- fulfillOrder action type signature accepts optional trackingNumber and carrier
- Order detail page shows tracking section only when trackingNumber AND carrier are present (existing behavior, line 225 of page.tsx)
</verification>

<success_criteria>
1. FulfillmentModal shows tracking number input field -- YES (already exists, unchanged)
2. Submitting with tracking number saves to WebsiteOrder -- YES (fulfillOrder action persists it)
3. Order detail displays tracking number -- YES (existing tracking section on detail page)
4. Tracking number optional -- YES (validation removed, submit enabled without tracking)
</success_criteria>

<output>
After completion, create `.planning/phases/13-fulfillment-tracking/13-01-SUMMARY.md`
</output>
