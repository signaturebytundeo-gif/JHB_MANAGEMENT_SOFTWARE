---
phase: 02-core-commerce-pages
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/page.tsx
  - src/components/home/HeroSection.tsx
  - src/components/home/BrandStory.tsx
  - src/components/home/SocialProof.tsx
  - src/components/home/HomeProductGrid.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage displays a hero section with headline, background visual treatment, and Shop Now CTA linking to /shop"
    - "Homepage shows all 4 products in a responsive grid with ProductCard components"
    - "Homepage includes a brand story section with Chef Anthony reference and 3-sentence origin story"
    - "Homepage includes a social proof section with testimonial quotes and star ratings"
    - "Homepage is mobile-responsive with all sections stacking vertically on small screens"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage assembling all sections"
      contains: "HeroSection"
    - path: "src/components/home/HeroSection.tsx"
      provides: "Full-width hero with headline and CTA"
      exports: ["default"]
    - path: "src/components/home/BrandStory.tsx"
      provides: "Chef Anthony brand story section"
      exports: ["default"]
    - path: "src/components/home/SocialProof.tsx"
      provides: "Testimonial quotes with star ratings"
      exports: ["default"]
    - path: "src/components/home/HomeProductGrid.tsx"
      provides: "Shoppable product grid using ProductCard"
      exports: ["default"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/home/HeroSection.tsx"
      via: "imports and renders HeroSection"
      pattern: "import.*HeroSection"
    - from: "src/components/home/HomeProductGrid.tsx"
      to: "src/components/ui/ProductCard.tsx"
      via: "maps products to ProductCard components"
      pattern: "ProductCard"
    - from: "src/components/home/SocialProof.tsx"
      to: "src/data/testimonials.ts"
      via: "imports testimonial data"
      pattern: "import.*testimonials"
    - from: "src/components/home/HeroSection.tsx"
      to: "/shop"
      via: "Shop Now CTA link"
      pattern: "href.*shop"
---

<objective>
Build the complete Homepage with hero section, shoppable product grid, brand story, and social proof sections.

Purpose: The Homepage is the primary landing page and first impression for customers. It must immediately communicate brand identity, showcase products, and drive users toward the shop.

Output: Fully functional Homepage at `/` with 4 sections: hero, product grid, brand story, and social proof.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-commerce-pages/02-RESEARCH.md
@.planning/phases/02-core-commerce-pages/02-01-SUMMARY.md

@src/app/page.tsx
@src/app/globals.css
@src/app/layout.tsx
@src/components/ui/ProductCard.tsx
@src/components/ui/StarRating.tsx
@src/data/products.ts
@src/data/testimonials.ts
@src/types/product.ts
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create homepage section components</name>
  <files>
    src/components/home/HeroSection.tsx
    src/components/home/BrandStory.tsx
    src/components/home/SocialProof.tsx
    src/components/home/HomeProductGrid.tsx
  </files>
  <action>
1. **Create HeroSection** at `src/components/home/HeroSection.tsx` (HOME-01):
   - Server Component (no 'use client')
   - Full-viewport height section: `relative min-h-[80vh] flex items-center justify-center`
   - Since product images may not exist yet, use a **gradient/color background** instead of `next/image` with a photo. Use a rich dark gradient: `bg-gradient-to-br from-brand-dark via-brand-dark to-brand-gold/10` with an overlay pattern
   - Center-aligned content:
     - Subtle "Est. 1994" or "Since 1994" text above headline in `text-brand-gold/60 uppercase tracking-[0.3em] text-sm mb-4`
     - Main headline `<h1>`: "30 Years of Flavor. One Legendary Sauce." in `text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white leading-tight`
     - Subheadline: "Authentic Jamaican Jerk Sauce, Crafted from Chef Anthony's Family Recipe" in `text-lg md:text-xl text-gray-300 mt-6 max-w-2xl mx-auto`
     - CTA button: `<Link href="/shop">` styled as `inline-block bg-brand-gold text-brand-dark font-bold text-lg px-10 py-4 rounded-lg hover:bg-brand-gold-light transition-colors mt-8` with text "Shop Now"
   - Add decorative elements: subtle gold border line or small hummingbird logo mark below CTA using `next/image` with `/images/hummingbird-logo.svg` at small size (24x24) with `opacity-40 mt-8`

2. **Create HomeProductGrid** at `src/components/home/HomeProductGrid.tsx` (HOME-02):
   - Server Component
   - Import `products` from `@/data/products` and `ProductCard` from `@/components/ui/ProductCard`
   - Section wrapper with heading:
     - Section: `py-16 sm:py-24 px-4`
     - Centered container: `max-w-7xl mx-auto`
     - Section header: `<h2>` "Our Products" in `text-3xl md:text-4xl font-bold text-white text-center mb-4`
     - Subtitle: "Handcrafted with 30 years of flavor expertise" in `text-gray-400 text-center mb-12`
   - Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`
   - Map all products to `<ProductCard product={product} />`
   - Below grid: centered "View All Products" link to `/shop` styled as `text-brand-gold hover:text-brand-gold-light font-medium mt-8 inline-block`

3. **Create BrandStory** at `src/components/home/BrandStory.tsx` (HOME-03):
   - Server Component
   - Section wrapper: `py-16 sm:py-24 px-4 bg-brand-dark`
   - Centered container: `max-w-5xl mx-auto`
   - Two-column layout on desktop: `grid grid-cols-1 md:grid-cols-2 gap-12 items-center`
   - Left column (image placeholder): A `div` with `aspect-square bg-gradient-to-br from-brand-gold/20 to-brand-gold/5 rounded-lg flex items-center justify-center`. Inside, place the hummingbird logo at larger size (120x120) as a placeholder for Chef Anthony's photo. Add text "Chef Anthony" below logo in gold.
   - Right column (story text):
     - Eyebrow text: "Our Story" in `text-brand-gold uppercase tracking-widest text-sm mb-4`
     - Heading: `<h2>` "From Our Kitchen to Yours" in `text-3xl md:text-4xl font-bold text-white mb-6`
     - 3-sentence story (from brand context): "What started as Chef Anthony's secret recipe at his first South Florida restaurant quickly became legendary. When 92% of customers started asking to buy the sauce by the bottle, we knew we had something special. Three restaurants and 30 years later, we're bringing that same authentic Jamaican flavor directly to your table." in `text-gray-300 leading-relaxed space-y-4` (use 3 `<p>` tags, one per sentence)
     - CTA link: "Read Our Full Story" linking to `/our-story` in `text-brand-gold hover:text-brand-gold-light font-medium mt-6 inline-block`

4. **Create SocialProof** at `src/components/home/SocialProof.tsx` (HOME-04):
   - Server Component
   - Import `testimonials` from `@/data/testimonials` and `StarRating` from `@/components/ui/StarRating`
   - Section wrapper: `py-16 sm:py-24 px-4`
   - Centered container: `max-w-6xl mx-auto`
   - Section header:
     - Heading: `<h2>` "What Our Customers Say" in `text-3xl md:text-4xl font-bold text-white text-center mb-12`
   - Testimonial grid: `grid grid-cols-1 md:grid-cols-2 gap-8`
   - Each testimonial card:
     - Container: `bg-white/5 border border-brand-gold/10 rounded-lg p-6`
     - StarRating with testimonial's rating
     - Quote text in `text-gray-300 italic mt-4 leading-relaxed` wrapped in `<blockquote>`
     - Author name in `text-white font-semibold mt-4` and location in `text-gray-500 text-sm`
  </action>
  <verify>
Verify all component files exist and have valid TypeScript:
```bash
cd /Users/rfmstaff/Desktop/jamaica-house-brand && npx tsc --noEmit
```
  </verify>
  <done>
HeroSection renders full-height hero with headline and Shop Now CTA. HomeProductGrid displays all 4 products using ProductCard. BrandStory shows Chef Anthony's 3-sentence origin story. SocialProof displays testimonial cards with star ratings. All components are Server Components with proper TypeScript typing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble Homepage from sections</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
1. **Replace existing page.tsx** (`src/app/page.tsx`) with the full Homepage:
   - Import all 4 section components from `@/components/home/`
   - Import `Metadata` from `next` and export metadata:
     ```typescript
     export const metadata: Metadata = {
       title: 'Jamaica House Brand - Original Jerk Sauce | 30 Years of Flavor',
       description: 'Authentic Jamaican jerk sauce crafted from Chef Anthony\'s 30-year family recipe. Shop our collection of all-natural, zero-calorie sauces.',
       openGraph: {
         title: 'Jamaica House Brand - Original Jerk Sauce',
         description: 'Authentic Jamaican jerk sauce crafted from Chef Anthony\'s 30-year family recipe.',
         type: 'website',
       },
     }
     ```
   - Page structure (Server Component, no 'use client'):
     ```tsx
     export default function Home() {
       return (
         <>
           <HeroSection />
           <HomeProductGrid />
           <BrandStory />
           <SocialProof />
         </>
       )
     }
     ```
   - Section order: Hero (top) -> Product Grid -> Brand Story -> Social Proof -> (Footer is already in layout.tsx from Plan 01)
   - Remove the existing placeholder content entirely
  </action>
  <verify>
Run dev server and verify homepage renders all sections:
```bash
cd /Users/rfmstaff/Desktop/jamaica-house-brand && npx next build 2>&1 | tail -20
```
Verify the homepage route compiles as static:
```bash
cd /Users/rfmstaff/Desktop/jamaica-house-brand && npx next build 2>&1 | grep -E "^\s*(\/|Route)" | head -10
```
  </verify>
  <done>
Homepage at `/` renders HeroSection, HomeProductGrid, BrandStory, and SocialProof in order. Page has proper metadata for SEO. Page compiles as static (no dynamic APIs used). Footer appears below all content via root layout.
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Homepage (`/`) route renders as static page
- Hero section shows headline "30 Years of Flavor. One Legendary Sauce." and Shop Now button linking to /shop
- Product grid displays all 4 products with images, names, prices, and ratings
- Brand story shows Chef Anthony's 3-sentence story
- Social proof shows testimonial cards with star ratings
- All sections are mobile-responsive (stack vertically on small screens)
- Footer appears at bottom of homepage
</verification>

<success_criteria>
Homepage fully assembled with hero (HOME-01), product grid (HOME-02), brand story (HOME-03), and social proof (HOME-04). Footer visible from root layout (HOME-05). All sections use dark premium aesthetic with gold accents. Page has SEO metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-commerce-pages/02-02-SUMMARY.md`
</output>
