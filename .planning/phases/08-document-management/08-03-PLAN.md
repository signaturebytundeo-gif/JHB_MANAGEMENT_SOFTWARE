---
phase: 08-document-management
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/command-center/src/components/documents/TemplateLibrary.tsx
  - apps/command-center/src/app/(dashboard)/dashboard/documents/page.tsx
  - apps/command-center/prisma/seed.ts
autonomous: true

must_haves:
  truths:
    - "Template library section displays downloadable templates for invoices, purchase orders, and wholesale agreements"
    - "Templates are visually distinct from user-uploaded documents"
    - "Each template has a download button"
    - "Template data is seeded in the database with isTemplate: true"
  artifacts:
    - path: "apps/command-center/src/components/documents/TemplateLibrary.tsx"
      provides: "Grid of template cards with download links"
      min_lines: 30
    - path: "apps/command-center/prisma/seed.ts"
      provides: "Template seed data with isTemplate: true"
      contains: "isTemplate"
  key_links:
    - from: "apps/command-center/src/components/documents/TemplateLibrary.tsx"
      to: "apps/command-center/src/app/actions/documents.ts"
      via: "receives templates from getTemplates() called in page.tsx"
      pattern: "getTemplates"
    - from: "apps/command-center/src/app/(dashboard)/dashboard/documents/page.tsx"
      to: "apps/command-center/src/components/documents/TemplateLibrary.tsx"
      via: "renders TemplateLibrary with template data"
      pattern: "TemplateLibrary"
---

<objective>
Build the Template Library: seed template records in the database, create a template grid component with download cards, and integrate it into the documents page as a distinct section.

Purpose: Delivers DOC-04 — users need quick access to downloadable templates for invoices, purchase orders, and wholesale agreements without navigating a separate upload flow.
Output: Template library section on the documents page with 3 seeded template entries and download functionality.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-document-management/08-RESEARCH.md
@.planning/phases/08-document-management/08-01-SUMMARY.md
@apps/command-center/prisma/seed.ts
@apps/command-center/src/app/actions/documents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template seed data and TemplateLibrary component</name>
  <files>
    apps/command-center/prisma/seed.ts
    apps/command-center/src/components/documents/TemplateLibrary.tsx
  </files>
  <action>
1. **seed.ts** — Add template document seeding at the end of the existing seed function. Create 3 template Document records with isTemplate: true:

```typescript
// Template documents — placeholder entries for DOC-04
const templates = [
  {
    name: 'Invoice Template',
    category: 'TEMPLATE' as const,
    currentBlobUrl: '', // Placeholder — real template file to be uploaded by admin
    isTemplate: true,
    description: 'Standard JHB invoice template with Net 30 terms, company branding, and line item breakdown',
  },
  {
    name: 'Purchase Order Template',
    category: 'TEMPLATE' as const,
    currentBlobUrl: '',
    isTemplate: true,
    description: 'Purchase order form for supplier and co-packer orders with delivery terms',
  },
  {
    name: 'Wholesale Agreement Template',
    category: 'TEMPLATE' as const,
    currentBlobUrl: '',
    isTemplate: true,
    description: 'Wholesale distribution agreement with pricing tiers, territory, and payment terms',
  },
];
```

Use upsert pattern (match by name + isTemplate: true) to avoid duplicates on re-seed. Set uploadedById to the admin user's ID (query first admin user). Since templates don't have real files yet, set currentBlobUrl to an empty string — the template card will show "Template file pending upload" when URL is empty, and a download button when URL is populated.

Also create one DocumentVersion per template (versionNumber: 1, blobUrl: '', fileName: 'pending', fileSize: 0).

Run `npx prisma db seed` after adding seed data.

2. **TemplateLibrary.tsx** — Server or client component (server is fine since no interactivity needed beyond links):
   - Receive templates array as prop (from getTemplates() in page.tsx)
   - Render a grid of Cards (shadcn Card component), 3 columns on desktop, 1 on mobile
   - Each card shows:
     - FileText icon (lucide-react) with document type color
     - Template name as heading
     - Description text (add a description field to the template data or derive from name)
     - If currentBlobUrl is non-empty: Download button (anchor tag, target="_blank", download attribute)
     - If currentBlobUrl is empty: "Pending upload" badge — admin can upload the actual template file via the main upload form
   - Distinct visual treatment from regular documents: use a different card background color or a "Template" badge
   - Since templates may not have real blob URLs initially (placeholder records), gracefully handle empty URLs

NOTE: The description field doesn't exist on the Document model. Instead of adding a schema field, use the template name to derive a description in the component, or pass descriptions as a static map keyed by template name. Keep it simple — no schema change needed for this.
  </action>
  <verify>
Run `npx prisma db seed` — succeeds and creates 3 template records. Query `SELECT * FROM Document WHERE isTemplate = true` (or via Prisma Studio) — shows 3 rows. TemplateLibrary component renders without TypeScript errors (`npx tsc --noEmit`).
  </verify>
  <done>
3 template records seeded in database with isTemplate: true. TemplateLibrary component renders grid of template cards with conditional download/pending state. No schema changes needed — uses existing Document model fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate TemplateLibrary into documents page</name>
  <files>
    apps/command-center/src/app/(dashboard)/dashboard/documents/page.tsx
  </files>
  <action>
Update the documents/page.tsx (created in Plan 02) to include the Template Library section:

1. Call getTemplates() server action alongside getDocuments() in the page component
2. Add a "Template Library" section below or above the documents list, visually separated with a heading and divider
3. Render `<TemplateLibrary templates={templates} />` in that section
4. The section should have a heading like "Template Library" with a subtitle "Download standard business document templates"
5. Use a Tabs component or separate sections to distinguish "My Documents" from "Templates" — simplest approach is two sections on the same page with clear headings

Layout order on the page:
- Page header: "Document Management"
- Upload form (collapsible or in dialog)
- Section 1: "Documents" — DocumentList component
- Section 2: "Template Library" — TemplateLibrary component

This task modifies a file also touched by Plan 02 Task 1. Since Plan 02 and Plan 03 are both Wave 2, if running sequentially (02 before 03), this task reads the file created by 02 and adds the template section. If 02 hasn't run yet, create the full page here with both sections.
  </action>
  <verify>
Navigate to /dashboard/documents — page shows both the documents list and the template library section. Template cards display with names, descriptions, and download/pending states. Page loads without errors.
  </verify>
  <done>
Documents page has two distinct sections: user documents with upload/filter/list, and template library with downloadable template cards. Templates are visually distinct from regular documents. All 4 DOC requirements are satisfied.
  </done>
</task>

</tasks>

<verification>
1. 3 template records exist in database (isTemplate: true)
2. Template library section visible on /dashboard/documents
3. Template cards show name, description, and download state
4. Templates are visually distinct from user-uploaded documents
5. Empty blob URLs show "pending upload" instead of broken download link
</verification>

<success_criteria>
- DOC-04: Template library provides downloadable templates for invoices, purchase orders, wholesale agreements
- Templates seeded in database, not uploaded via UI
- Template section integrated into documents page
- Graceful handling of placeholder templates (no real files yet)
</success_criteria>

<output>
After completion, create `.planning/phases/08-document-management/08-03-SUMMARY.md`
</output>
