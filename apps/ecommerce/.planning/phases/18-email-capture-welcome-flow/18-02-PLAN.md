---
phase: 18-email-capture-welcome-flow
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/layout/Footer.tsx
  - src/components/ExitIntentPopup.tsx
  - src/lib/subscribe-store.ts
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Customer can enter email in the site footer and receive confirmation of subscription"
    - "An exit-intent popup appears when the customer moves to leave the page, offering a discount for subscribing"
    - "Subscribing via footer or popup both trigger the welcome email flow"
    - "Popup does not reappear after dismissal or successful subscription (localStorage persistence)"
    - "Footer form shows success/error feedback inline"
  artifacts:
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer with email subscription form"
      contains: "subscribe"
    - path: "src/components/ExitIntentPopup.tsx"
      provides: "Exit-intent email capture popup"
      contains: "mouseleave"
    - path: "src/lib/subscribe-store.ts"
      provides: "Zustand store for subscription state persistence"
      exports: ["useSubscribeStore"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with ExitIntentPopup"
      contains: "ExitIntentPopup"
  key_links:
    - from: "src/components/layout/Footer.tsx"
      to: "/api/subscribe"
      via: "fetch POST"
      pattern: "fetch.*api/subscribe"
    - from: "src/components/ExitIntentPopup.tsx"
      to: "/api/subscribe"
      via: "fetch POST"
      pattern: "fetch.*api/subscribe"
    - from: "src/components/ExitIntentPopup.tsx"
      to: "src/lib/subscribe-store.ts"
      via: "useSubscribeStore"
      pattern: "useSubscribeStore"
    - from: "src/components/layout/Footer.tsx"
      to: "src/lib/subscribe-store.ts"
      via: "useSubscribeStore"
      pattern: "useSubscribeStore"
---

<objective>
Build the footer email form and exit-intent popup, both wired to POST /api/subscribe.

Purpose: Provide two email capture touchpoints — passive (footer form always visible) and active (exit-intent popup with discount incentive) — that trigger the welcome email flow from Plan 01.
Output: Footer with working email input, exit-intent popup with email capture, shared subscription state that prevents duplicate popups/submissions.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-email-capture-welcome-flow/18-01-SUMMARY.md
@src/components/layout/Footer.tsx
@src/app/layout.tsx
@src/components/FreeSamplePopup.tsx
@src/lib/sample-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create subscribe store and add email form to Footer</name>
  <files>
    src/lib/subscribe-store.ts
    src/components/layout/Footer.tsx
  </files>
  <action>
1. Create `src/lib/subscribe-store.ts` — a Zustand store with persist middleware (same pattern as cart-store and sample-store):
   - State: `{ isSubscribed: boolean; popupDismissed: boolean; dismissedAt: number | null }`
   - Actions: `subscribe()` sets isSubscribed=true, `dismissPopup()` sets popupDismissed=true + dismissedAt=Date.now()
   - Helper export: `isDismissedRecently()` — returns true if dismissedAt is within last 7 days (604800000 ms)
   - Persist to localStorage key `jhb-subscribe-state`
   - Use `skipHydration: true` pattern (same as other stores in this codebase)

2. Modify `src/components/layout/Footer.tsx`:
   - Convert to a Client Component ('use client') — needed for form state and store access
   - Keep the existing 4-column grid structure exactly as-is
   - Replace the "Follow Us" column (Column 4) content: keep the social links but ADD an email subscription form ABOVE them
   - Email form structure:
     - Heading: "Get 10% Off" (h4, font-semibold text-white mb-2)
     - Subtext: "Subscribe for exclusive deals" (text-gray-400 text-sm mb-3)
     - Form: horizontal flex container with email input + submit button
       - Input: `type="email"` placeholder="Your email", dark bg (bg-white/10), border border-brand-gold/20, rounded-l-lg, text-white, placeholder-gray-500, px-3 py-2, flex-1
       - Button: bg-brand-gold text-brand-dark font-semibold px-4 py-2 rounded-r-lg hover:bg-brand-gold-light transition-colors, text "Join"
     - Loading state: button text changes to "..." and is disabled during fetch
     - Success state: replace entire form with checkmark + "You're in! Check your inbox." (text-brand-gold)
     - Error state: small red text below form (text-red-400 text-xs mt-1)
   - On submit: POST to /api/subscribe with { email }, call `useSubscribeStore().subscribe()` on success
   - If `useSubscribeStore().isSubscribed` is true on mount, show success state immediately (already subscribed)
   - Keep all existing footer content (brand column, quick links, contact, social links, payment methods, bottom bar) UNCHANGED
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors. Run `npm run build` — build succeeds. Footer.tsx contains 'use client', email input, fetch to /api/subscribe.
  </verify>
  <done>Footer displays email subscription form with "Get 10% Off" heading. Form submits to /api/subscribe, shows success/error states, persists subscription status via Zustand store.</done>
</task>

<task type="auto">
  <name>Task 2: Create exit-intent popup and wire into root layout</name>
  <files>
    src/components/ExitIntentPopup.tsx
    src/app/layout.tsx
  </files>
  <action>
1. Create `src/components/ExitIntentPopup.tsx` — a 'use client' component:
   - Uses Headless UI Dialog (already installed) for accessible modal — same pattern as FreeSamplePopup.tsx
   - Exit-intent detection: listen for `mouseleave` on document with `event.clientY < 0` (cursor moves above viewport). Desktop only — this naturally doesn't fire on mobile/touch devices, which is fine.
   - Popup appears ONLY if:
     - User is NOT already subscribed (`!useSubscribeStore().isSubscribed`)
     - Popup was NOT dismissed recently (`!isDismissedRecently()` from subscribe-store)
     - At least 5 seconds have passed since page load (setTimeout guard to prevent immediate trigger)
     - Popup hasn't already been shown this session (local useState flag)
   - Design (matches JHB dark + gold aesthetic, similar to FreeSamplePopup):
     - Dark panel: bg-brand-dark border border-brand-gold/30 rounded-2xl max-w-md w-full p-8 shadow-2xl
     - Overlay: bg-black/60
     - Gold accent icon or decorative element at top (use a simple percentage "10%" in large gold text as the visual hook)
     - Title: "Wait! Don't Leave Empty-Handed" (text-2xl font-bold text-white text-center)
     - Subtitle: "Get 10% off your first order when you join our list." (text-gray-300 text-center mb-6)
     - Email input: full width, bg-white/10, border border-brand-gold/20, rounded-lg, text-white, placeholder-gray-500, px-4 py-3, placeholder "Enter your email"
     - CTA button: full width, bg-brand-gold text-brand-dark font-bold text-lg py-4 rounded-lg hover:bg-brand-gold-light, text "Unlock My 10% Off"
     - Dismiss link: "No thanks, I'll pay full price" (text-gray-400 text-sm, centered below button)
     - Loading state: button shows "Sending..." and is disabled
     - Success state: replace form content with checkmark + "You're in! Check your inbox for your code." then auto-close after 2 seconds
   - On submit: POST to /api/subscribe with { email }. On success, call `useSubscribeStore().subscribe()`.
   - On dismiss: call `useSubscribeStore().dismissPopup()`, close dialog.

2. Modify `src/app/layout.tsx`:
   - Import ExitIntentPopup from '@/components/ExitIntentPopup'
   - Add `<ExitIntentPopup />` after `<UpsellModal />` (before the Suspense-wrapped MetaPixel)
   - No other changes to layout.tsx
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors. Run `npm run build` — build succeeds. ExitIntentPopup.tsx exists with mouseleave handler. layout.tsx imports and renders ExitIntentPopup.
  </verify>
  <done>Exit-intent popup captures email when user moves to leave page. Popup respects dismissal (7-day cooldown) and subscription status. Both footer and popup use the same subscribe store and API endpoint.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- Footer shows email input form with "Get 10% Off" heading
- ExitIntentPopup exists with mouseleave detection (clientY < 0)
- Both components POST to /api/subscribe
- subscribe-store persists to localStorage
- ExitIntentPopup is rendered in root layout
- Already-subscribed users see success state in footer, no popup
- Dismissed popup doesn't reappear for 7 days
</verification>

<success_criteria>
- Customer can enter email in footer and see confirmation
- Exit-intent popup appears on cursor leave (desktop)
- Both touchpoints trigger the same /api/subscribe endpoint
- Subscription state persists across page refreshes (localStorage)
- Popup does not reappear after dismissal or subscription
- Build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-email-capture-welcome-flow/18-02-SUMMARY.md`
</output>
