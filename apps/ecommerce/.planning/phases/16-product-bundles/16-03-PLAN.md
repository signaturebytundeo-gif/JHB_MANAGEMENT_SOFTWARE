---
phase: 16-product-bundles
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - src/app/api/checkout/route.ts
autonomous: true

must_haves:
  truths:
    - "Pre-defined bundle orders create a single line item in Stripe checkout with bundle name and bundle price"
    - "Build-your-own bundle orders show individual items plus a discount line item in Stripe checkout"
    - "Bundle orders appear in command-center with accurate line item records"
  artifacts:
    - path: "src/app/api/checkout/route.ts"
      provides: "Checkout API handling pre-defined bundles as single line items and build-your-own discount as a negative-amount line item"
  key_links:
    - from: "src/app/api/checkout/route.ts"
      to: "Stripe Checkout Session"
      via: "line_items array with bundle name or discount line"
      pattern: "Bundle|bundle discount"
    - from: "command-center webhook"
      to: "src/app/api/checkout/route.ts"
      via: "Stripe listLineItems reads whatever line items checkout created"
      pattern: "listLineItems"
---

<objective>
Wire bundle pricing through the Stripe checkout API so pre-defined bundles appear as correctly-named single line items and build-your-own discounts appear as a discount line item.

Purpose: Fulfills BNDL-04 (bundle orders create correct line items in Stripe checkout and command-center order records). The command-center webhook already reads line items from Stripe via listLineItems, so no command-center code changes needed — as long as the ecommerce checkout creates correct Stripe line items, the command-center will record them accurately.

Output: Checkout API correctly handles both pre-defined bundle cart items and build-your-own discount, producing clean Stripe line items that flow through to command-center.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-product-bundles/16-01-SUMMARY.md
@.planning/phases/16-product-bundles/16-02-SUMMARY.md
@src/app/api/checkout/route.ts
@src/lib/bundle-discount.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update checkout API to handle bundles and build-your-own discount</name>
  <files>src/app/api/checkout/route.ts</files>
  <action>
1. Update the CheckoutItem interface to include `isBundle?: boolean` to match the CartItem type from cart-store.ts.

2. Update the request body parsing to accept `bundleDiscount` (number, in cents) alongside `items`:
   ```typescript
   const { items, bundleDiscount = 0 }: { items: CheckoutItem[]; bundleDiscount?: number } = await request.json()
   ```

3. Pre-defined bundle handling — the existing line_items mapping already works for bundles because a bundle is just a cart item with a name, price, and size. The line item will show:
   - name: "The Starter Bundle (2oz + 5oz + 12oz)" — uses the existing `${item.name} (${item.size})` pattern
   - unit_amount: the bundle price (e.g., 2499 for $24.99)
   - This is correct — no special handling needed for pre-defined bundles in line_items mapping.

4. Build-your-own discount handling — after building the regular line_items array, if `bundleDiscount > 0`:
   - Server-side validation: import `calculateBundleDiscount` from '@/lib/bundle-discount' and re-calculate the discount from the items array to prevent client-side tampering. Use the server-calculated discount, NOT the client-sent value.
   - If server-calculated discount > 0, add a coupon/discount. Stripe Checkout supports `discounts` with a coupon, but creating coupons on the fly is complex. Instead, use a simpler approach: add a negative-value line item is NOT supported by Stripe (unit_amount must be positive).
   - **Correct approach**: Use Stripe Checkout's `discounts` feature with an inline coupon. Create a one-time coupon via the Stripe API:
     ```typescript
     const coupon = await stripe.coupons.create({
       amount_off: serverDiscount,
       currency: 'usd',
       name: `Build Your Own Bundle (${BUNDLE_DISCOUNT_PERCENT}% off)`,
       duration: 'once',
     })
     ```
     Then add to the session: `discounts: [{ coupon: coupon.id }]`
   - Only create the coupon and add discounts if serverDiscount > 0

5. Update metadata to include bundle info:
   - Add `hasBundles: items.some(i => i.isBundle) ? 'true' : 'false'`
   - Add `bundleDiscount: serverDiscount > 0 ? serverDiscount.toString() : '0'`

6. Shipping calculation — bundles should count toward the $50 free shipping threshold like any other paid item. The existing `paidItemsTotal` calculation already sums all non-free-sample items by price * quantity, which includes bundles. No change needed for shipping.

7. Command-center compatibility — the command-center webhook at `/api/webhooks/stripe/route.ts` uses `stripe.checkout.sessions.listLineItems()` to get line items. It filters out "Shipping & Handling" and maps the rest. Pre-defined bundles will appear as regular line items with their bundle name. The discount coupon will appear in the session's total_details but NOT as a line item — so the command-center will see the correct product line items and the orderTotal will reflect the discounted amount (session.amount_total already includes discounts). No command-center changes needed.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Run `npm run build` to confirm build passes.

Manual test scenarios (if dev server available):
1. Add Starter Bundle to cart, checkout — Stripe shows "The Starter Bundle (2oz + 5oz + 12oz)" at $24.99
2. Add 3 individual items to cart, checkout — Stripe shows 3 individual line items with a coupon discount applied
3. Add 2 individual items to cart (no discount), checkout — Stripe shows 2 line items, no discount
4. Mix: add 1 bundle + 3 individual items — Stripe shows bundle line + 3 individual lines + discount on individuals only
  </verify>
  <done>Checkout API handles pre-defined bundles as single line items and build-your-own discounts via Stripe coupons. Server-side discount validation prevents tampering. Command-center receives accurate order data via existing webhook without code changes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` passes
- Pre-defined bundle creates single Stripe line item with bundle name and price
- Build-your-own discount creates Stripe coupon with correct percentage amount
- Server re-calculates discount (does not trust client-sent value)
- Shipping threshold still works correctly with bundles
- Command-center webhook receives accurate line items (no code changes needed)
</verification>

<success_criteria>
- Bundle orders create correct line items in Stripe checkout with bundle name and price (BNDL-04)
- Build-your-own discount applied as Stripe coupon, visible in checkout
- Command-center order records reflect accurate bundle line items and totals
- Server-side validation prevents discount manipulation
</success_criteria>

<output>
After completion, create `.planning/phases/16-product-bundles/16-03-SUMMARY.md`
</output>
