---
phase: 07-dashboards-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/command-center/src/app/actions/dashboard-kpis.ts
  - apps/command-center/src/app/(dashboard)/dashboard/page.tsx
  - apps/command-center/src/components/dashboard/MTDRevenueVsTarget.tsx
  - apps/command-center/src/components/dashboard/ARAgingWidget.tsx
autonomous: true

must_haves:
  truths:
    - "Executive dashboard shows Today's Revenue combining sales + operator orders"
    - "MTD Revenue shows progress bar vs $100K monthly target with percentage"
    - "Units Produced shows progress bar vs 15,000 capacity"
    - "Current Inventory Value displays aggregated dollar total"
    - "Open Orders counts both WebsiteOrder AND operator Order records"
    - "Accounts Receivable highlights overdue amounts separately"
    - "Quick action buttons New Order, Log Batch, Record Expense, Transfer, Invoice are all enabled with correct hrefs"
  artifacts:
    - path: "apps/command-center/src/app/actions/dashboard-kpis.ts"
      provides: "Combined KPI data fetching for executive dashboard"
      exports: ["getDashboardKPIs"]
    - path: "apps/command-center/src/components/dashboard/MTDRevenueVsTarget.tsx"
      provides: "Progress bar showing MTD revenue vs $100K target"
      contains: "style.*width"
    - path: "apps/command-center/src/components/dashboard/ARAgingWidget.tsx"
      provides: "Accounts receivable with overdue highlighting"
      contains: "overdue"
    - path: "apps/command-center/src/app/(dashboard)/dashboard/page.tsx"
      provides: "Wired executive dashboard with all 6 KPI cards + quick actions"
      contains: "getDashboardKPIs"
  key_links:
    - from: "apps/command-center/src/app/(dashboard)/dashboard/page.tsx"
      to: "apps/command-center/src/app/actions/dashboard-kpis.ts"
      via: "server component import"
      pattern: "getDashboardKPIs"
    - from: "apps/command-center/src/app/actions/dashboard-kpis.ts"
      to: "prisma"
      via: "database queries"
      pattern: "db\\.(websiteOrder|order|sale|batch|inventoryMovement)"
---

<objective>
Wire the executive dashboard with complete, accurate KPI data and enable all quick action buttons.

Purpose: DASH-01 through DASH-07 — the executive dashboard is the primary landing page for ADMIN/MANAGER roles. KPI cards already exist but some use incomplete data (Open Orders only counts WebsiteOrder, not operator Order). Quick action buttons are partially disabled.

Output: Fully wired executive dashboard with 6 accurate KPI cards (including progress bars for MTD Revenue and Units Produced), overdue AR highlighting, and 5 enabled quick action buttons.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboards-reporting/07-RESEARCH.md

@apps/command-center/src/app/(dashboard)/dashboard/page.tsx
@apps/command-center/src/app/actions/sales.ts
@apps/command-center/src/app/actions/orders.ts
@apps/command-center/src/app/actions/production.ts
@apps/command-center/src/app/actions/inventory.ts
@apps/command-center/src/app/actions/financial-reports.ts
@apps/command-center/src/components/dashboard/KPICard.tsx
@apps/command-center/src/components/production/CapacityMetrics.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create getDashboardKPIs server action with complete data</name>
  <files>apps/command-center/src/app/actions/dashboard-kpis.ts</files>
  <action>
Create a new server action file `dashboard-kpis.ts` with a single `getDashboardKPIs()` function that returns all 6 KPI values in one call using `Promise.all`. This replaces the separate `getProductionMetrics`, `getSalesMetrics`, `getOrderMetrics` calls on the dashboard page.

The function must:
1. **Today's Revenue (DASH-01):** Sum `Sale.totalAmount` for today + sum `Order.totalAmount` for today where status is not CANCELLED. Use `startOfDay`/`endOfDay` from date-fns.
2. **MTD Revenue (DASH-02):** Same as above but `startOfMonth`/`endOfMonth`. Also return `targetRevenue` from `getMonthlyProjection(new Date().getFullYear())` in `financial-reports.ts` (returns $100K/mo for 2026). Calculate `mtdRevenuePercent = (mtdRevenue / targetRevenue) * 100`.
3. **Units Produced (DASH-03):** Count `Batch.totalUnits` where status is RELEASED and createdAt is this month. Return `unitsProduced`, `batchCount`, `capacityTarget: 15000`, `capacityPercent`.
4. **Inventory Value (DASH-04):** Query all products with their pricingTiers. For each product, get available stock from `BatchAllocation` (sum of allocatedUnits minus fulfilled). Multiply by the lowest `PricingTier.unitPrice` per product (conservative valuation). Return total dollar value.
5. **Open Orders (DASH-05) — FIX THE GAP:** Count BOTH `db.websiteOrder.count({ where: { status: { in: ['NEW', 'PROCESSING'] } } })` AND `db.order.count({ where: { status: { in: ['DRAFT', 'CONFIRMED', 'PROCESSING'] } } })`. Sum both counts. Also sum their `totalAmount` values for total open order value.
6. **Accounts Receivable (DASH-06):** Query invoices with status SENT or OVERDUE. Return `totalAR`, `overdueAmount` (where status is OVERDUE or dueDate < now), `overdueCount`.

Import `verifySession` from `@/lib/dal`. Call it first. Import `db` from `@/lib/db`.

Return a typed object:
```typescript
type DashboardKPIs = {
  todayRevenue: number;
  todayOrderCount: number;
  mtdRevenue: number;
  mtdOrderCount: number;
  targetRevenue: number;
  mtdRevenuePercent: number;
  unitsProduced: number;
  batchCount: number;
  capacityTarget: number;
  capacityPercent: number;
  inventoryValue: number;
  openOrderCount: number;
  openOrderValue: number;
  totalAR: number;
  overdueAmount: number;
  overdueCount: number;
};
```
  </action>
  <verify>Run `npx tsc --noEmit` from apps/command-center — no type errors in dashboard-kpis.ts</verify>
  <done>getDashboardKPIs returns all 6 KPI data points in a single server action call, including the fixed Open Orders count that includes both WebsiteOrder and operator Order records</done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard page with new KPIs, progress bars, AR widget, and quick actions</name>
  <files>
    apps/command-center/src/app/(dashboard)/dashboard/page.tsx
    apps/command-center/src/components/dashboard/MTDRevenueVsTarget.tsx
    apps/command-center/src/components/dashboard/ARAgingWidget.tsx
  </files>
  <action>
**MTDRevenueVsTarget.tsx** — Create a server component that renders:
- KPICard-style card with title "MTD Revenue"
- The dollar amount prominently displayed
- A progress bar below (copy the pattern from `CapacityMetrics.tsx`): div with `h-2 w-full rounded-full bg-muted overflow-hidden` containing a colored inner div with `style={{ width: \`${cappedPercent}%\` }}`. Color: green if >= 80% of target, amber if >= 50%, red if < 50%.
- Subtitle: `${percent.toFixed(1)}% of $${(target/1000).toFixed(0)}K target`
- Props: `{ mtdRevenue: number; targetRevenue: number; mtdRevenuePercent: number }`

**ARAgingWidget.tsx** — Create a component that renders:
- KPICard-style card with title "Accounts Receivable"
- Total AR as the main value
- If overdueAmount > 0, show a red-highlighted line: `${overdueCount} overdue ($${overdueAmount.toFixed(2)})`
- If overdueAmount === 0, show green text: "No overdue invoices"
- Props: `{ totalAR: number; overdueAmount: number; overdueCount: number }`

**Dashboard page.tsx** — Refactor to use `getDashboardKPIs()`:
1. Replace the three separate `getProductionMetrics`, `getSalesMetrics`, `getOrderMetrics` calls in `KPICards` with a single `getDashboardKPIs()` call.
2. Keep the existing `getInventoryAggregation()` call for the Inventory Status section (it powers `InventoryStatusSummary` which is a separate section).
3. Update the 6 KPI cards grid for ADMIN/MANAGER:
   - **Today's Revenue:** `KPICard` with `kpis.todayRevenue` and `${kpis.todayOrderCount} orders today`
   - **MTD Revenue:** Replace KPICard with `<MTDRevenueVsTarget mtdRevenue={kpis.mtdRevenue} targetRevenue={kpis.targetRevenue} mtdRevenuePercent={kpis.mtdRevenuePercent} />`
   - **Units Produced:** Keep KPICard but add a progress bar div below the subtitle (same CapacityMetrics pattern). Color by percent: green >= 80, amber >= 50, red < 50.
   - **Inventory Value:** NEW card — `KPICard` with `$${kpis.inventoryValue.toFixed(2)}` and subtitle "Across all locations"
   - **Open Orders:** `KPICard` with `kpis.openOrderCount` and subtitle `$${kpis.openOrderValue.toFixed(2)} total value`
   - **Accounts Receivable:** Replace KPICard with `<ARAgingWidget totalAR={kpis.totalAR} overdueAmount={kpis.overdueAmount} overdueCount={kpis.overdueCount} />`
4. Update SALES_REP cards similarly where applicable (keep existing metrics but source from getDashboardKPIs).
5. **Quick Actions (DASH-07):** Update the ADMIN quick actions array:
   - `{ label: 'New Order', href: '/dashboard/orders/new', icon: Plus, enabled: true }`
   - `{ label: 'Log Batch', href: '/dashboard/production/new', icon: PackagePlus, enabled: true }`
   - `{ label: 'Record Expense', href: '/dashboard/finance/expenses', icon: Receipt, enabled: true }`
   - `{ label: 'Transfer', href: '/dashboard/inventory/transfers', icon: ArrowLeftRight, enabled: true }`
   - `{ label: 'Invoice', href: '/dashboard/finance/invoices', icon: DollarSign, enabled: true }`
   All 5 buttons enabled with valid hrefs. Remove the `phase` property and disabled tooltip logic for these 5 buttons. Keep the MANAGER and SALES_REP action sets appropriately (MANAGER gets all except Invoice, SALES_REP keeps existing).
  </action>
  <verify>Run `npx tsc --noEmit` from apps/command-center — no type errors. Run `npm run build` — page builds without errors.</verify>
  <done>Executive dashboard shows all 6 KPIs with accurate data, MTD Revenue and Units Produced have progress bars, AR highlights overdue amounts, all 5 quick action buttons are enabled and linked to correct pages</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds from apps/command-center
- Dashboard page imports getDashboardKPIs (not the old separate actions)
- Open Orders count includes both WebsiteOrder and operator Order
- MTD Revenue shows progress bar with target percentage
- Quick actions array has 5 entries for ADMIN, all with `enabled: true` and valid `href`
- ARAgingWidget conditionally highlights overdue amounts
</verification>

<success_criteria>
DASH-01: Today's Revenue card shows combined sales + orders revenue
DASH-02: MTD Revenue card shows progress bar vs $100K target
DASH-03: Units Produced card shows progress bar vs 15,000 capacity
DASH-04: Inventory Value card shows aggregated dollar total
DASH-05: Open Orders counts both WebsiteOrder AND operator Order
DASH-06: Accounts Receivable highlights overdue invoices
DASH-07: All 5 quick action buttons enabled with correct links
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboards-reporting/07-01-SUMMARY.md`
</output>
