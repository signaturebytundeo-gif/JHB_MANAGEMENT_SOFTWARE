---
phase: 07-dashboards-reporting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/command-center/src/app/actions/investor-metrics.ts
  - apps/command-center/src/app/(dashboard)/dashboard/investor/page.tsx
  - apps/command-center/src/components/investor/RevenueTrendChart.tsx
  - apps/command-center/src/components/investor/ChannelPieChart.tsx
  - apps/command-center/src/components/investor/CapacityGauge.tsx
  - apps/command-center/src/components/investor/FinancialHealthCards.tsx
  - apps/command-center/src/components/investor/DarkModeToggle.tsx
  - apps/command-center/src/components/investor/PrintButton.tsx
autonomous: true

must_haves:
  truths:
    - "Investor sees revenue trend line chart with monthly data and projections"
    - "Investor sees channel diversification pie chart"
    - "Investor sees production capacity utilization gauge"
    - "Investor sees financial health metrics (gross margin, revenue vs projections)"
    - "Investor sees ownership structure with 55/30/15 split"
    - "Investor can toggle dark mode"
    - "Investor can export dashboard as PDF via print"
  artifacts:
    - path: "apps/command-center/src/app/actions/investor-metrics.ts"
      provides: "All investor dashboard data fetching"
      exports: ["getInvestorMetrics"]
    - path: "apps/command-center/src/components/investor/RevenueTrendChart.tsx"
      provides: "Monthly revenue trend line chart"
      contains: "use client"
    - path: "apps/command-center/src/components/investor/ChannelPieChart.tsx"
      provides: "Channel diversification pie chart"
      contains: "use client"
    - path: "apps/command-center/src/components/investor/CapacityGauge.tsx"
      provides: "Production capacity utilization display"
      contains: "style.*width"
    - path: "apps/command-center/src/components/investor/DarkModeToggle.tsx"
      provides: "Dark mode toggle button"
      contains: "useTheme"
    - path: "apps/command-center/src/components/investor/PrintButton.tsx"
      provides: "PDF export via window.print()"
      contains: "window.print"
    - path: "apps/command-center/src/app/(dashboard)/dashboard/investor/page.tsx"
      provides: "Fully wired investor dashboard"
      contains: "getInvestorMetrics"
  key_links:
    - from: "apps/command-center/src/app/(dashboard)/dashboard/investor/page.tsx"
      to: "apps/command-center/src/app/actions/investor-metrics.ts"
      via: "server component import"
      pattern: "getInvestorMetrics"
    - from: "apps/command-center/src/components/investor/RevenueTrendChart.tsx"
      to: "recharts"
      via: "client component import"
      pattern: "LineChart"
    - from: "apps/command-center/src/components/investor/DarkModeToggle.tsx"
      to: "next-themes"
      via: "useTheme hook"
      pattern: "useTheme"
---

<objective>
Wire the investor dashboard with live data, recharts visualizations, dark mode toggle, and PDF export.

Purpose: INVST-01 through INVST-07 — the investor portal currently shows placeholder "Coming in Phase X" values. Replace all placeholders with real data from existing server actions, add revenue trend line chart, channel diversification pie chart, capacity gauge, financial health metrics, and enable dark mode toggle + PDF export.

Output: Fully functional read-only investor dashboard with 5 data sections, 2 interactive charts, dark mode toggle, and print-to-PDF export.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboards-reporting/07-RESEARCH.md

@apps/command-center/src/app/(dashboard)/dashboard/investor/page.tsx
@apps/command-center/src/app/actions/financial-reports.ts
@apps/command-center/src/app/actions/production.ts
@apps/command-center/src/components/finance/RevenueVsProjectionChart.tsx
@apps/command-center/src/components/finance/RevenueByChannelChart.tsx
@apps/command-center/src/components/production/CapacityMetrics.tsx
@apps/command-center/src/components/ui/chart.tsx
@apps/command-center/src/components/theme-provider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create investor metrics action and chart components</name>
  <files>
    apps/command-center/src/app/actions/investor-metrics.ts
    apps/command-center/src/components/investor/RevenueTrendChart.tsx
    apps/command-center/src/components/investor/ChannelPieChart.tsx
    apps/command-center/src/components/investor/CapacityGauge.tsx
    apps/command-center/src/components/investor/FinancialHealthCards.tsx
    apps/command-center/src/components/investor/DarkModeToggle.tsx
    apps/command-center/src/components/investor/PrintButton.tsx
  </files>
  <action>
**investor-metrics.ts** — Create a `getInvestorMetrics()` server action that calls `verifySession()` then fetches all investor data in parallel using `Promise.all`:

1. **Revenue Trend (INVST-01):** Call `getMonthlyRevenueTrend(currentYear)` from `financial-reports.ts`. Also compute YTD revenue (sum of all monthly revenues) and YoY growth (compare to prior year if data exists, else show "N/A").
2. **Channel Diversification (INVST-02):** Call `getRevenueByChannel(new Date())` from `financial-reports.ts`.
3. **Production Capacity (INVST-03):** Call `getProductionMetrics()` from `production.ts`. Return `utilizationPercent`, `totalUnits`, `target`.
4. **Financial Health (INVST-04):** Call `getPnLReport({ year: currentYear, month: currentMonth, period: 'monthly' })` from `financial-reports.ts`. Extract gross margin %, net margin %, total revenue, total expenses. Also include monthly projection from `getMonthlyProjection(currentYear)`.
5. **Active Customers count:** `db.customer.count({ where: { isActive: true } })`.
6. **Active Channels count:** `db.salesChannel.count({ where: { isActive: true } })`.

Return typed object with all data needed by the page.

**RevenueTrendChart.tsx** — `'use client'` component. Props: `{ data: MonthlyRevenueTrendItem[] }` (type imported from financial-reports.ts). Render a `LineChart` inside `ChartContainer` (follow pattern from `RevenueVsProjectionChart.tsx`). Two lines: `revenue` (solid) and `projection` (dashed). X axis: month. Y axis: dollar amount. Include `ChartTooltip` with `ChartTooltipContent`.

**ChannelPieChart.tsx** — `'use client'` component. Props: `{ data: { channel: string; revenue: number }[] }`. Render a `PieChart` inside `ChartContainer`. Use `hsl(var(--chart-1))` through `hsl(var(--chart-5))` colors. Include tooltip. Show channel labels.

**CapacityGauge.tsx** — Server component (no recharts needed). Props: `{ utilization: number; totalUnits: number; target: number }`. Render a progress bar (same CapacityMetrics.tsx pattern): `h-3 w-full rounded-full bg-muted overflow-hidden` with colored inner div. Green >= 70%, amber >= 40%, red < 40%. Show "{utilization}% of {target.toLocaleString()} unit capacity" text.

**FinancialHealthCards.tsx** — Server component. Props: `{ grossMargin: number; netMargin: number; totalRevenue: number; totalExpenses: number; monthlyProjection: number }`. Render a grid of 4 metric cards showing Gross Margin %, Net Margin %, Revenue vs Projection delta, and Expense ratio.

**DarkModeToggle.tsx** — `'use client'` component. Uses `useTheme` from `next-themes`. Renders a button with `Moon`/`Sun` icon from lucide-react. Add `print:hidden` class so it doesn't appear in PDF export. Add `className="print:hidden"` to the button.

**PrintButton.tsx** — `'use client'` component. Renders a button with `Download`/`Printer` icon. `onClick={() => window.print()}`. Add `print:hidden` class. Label: "Export PDF".
  </action>
  <verify>Run `npx tsc --noEmit` from apps/command-center — no type errors in any new files</verify>
  <done>All 7 new files created with correct types, chart components use 'use client', server components don't import recharts</done>
</task>

<task type="auto">
  <name>Task 2: Wire investor dashboard page with live data and all components</name>
  <files>
    apps/command-center/src/app/(dashboard)/dashboard/investor/page.tsx
  </files>
  <action>
Rewrite the investor dashboard page to replace ALL placeholder values with live data.

The page is a Server Component that calls `getInvestorMetrics()` and passes data down as props.

**Layout structure:**

1. **Header row** — Title "Investor Dashboard" on left. On right: `<DarkModeToggle />` and `<PrintButton />` side by side. Both have `print:hidden`.

2. **Revenue Overview section (INVST-01):**
   - 3 KPICards in a grid: MTD Revenue, YTD Revenue, Revenue Growth (YoY % or "N/A" for first year)
   - Below cards: `<RevenueTrendChart data={metrics.revenueTrend} />` showing monthly line chart

3. **Channel Diversification section (INVST-02):**
   - `<ChannelPieChart data={metrics.channelRevenue} />` on left
   - Summary stats on right: number of active channels, top channel by revenue

4. **Production Capacity section (INVST-03):**
   - `<CapacityGauge utilization={metrics.capacityUtilization} totalUnits={metrics.totalUnitsProduced} target={metrics.capacityTarget} />`

5. **Financial Health section (INVST-04):**
   - `<FinancialHealthCards grossMargin={metrics.grossMargin} netMargin={metrics.netMargin} totalRevenue={metrics.totalRevenue} totalExpenses={metrics.totalExpenses} monthlyProjection={metrics.monthlyProjection} />`

6. **Ownership Structure section (INVST-05):**
   - Keep the existing equity table exactly as-is (Anthony 55%, Olatunde 30%, Olutomiwa 15%). This is static, correct content. Do NOT change the ownership percentages.

**Print CSS:**
- Add `print:hidden` to the sidebar/layout elements. Check if the dashboard layout.tsx already handles this. If not, add a wrapper: `<div className="print:p-0">` around the content and ensure the page header buttons have `print:hidden`.
- The entire content section should be visible in print. Only hide interactive elements (buttons, toggles).

**Suspense:** Wrap the data-dependent sections in `<Suspense fallback={<DashboardSkeleton />}>` using the existing skeleton component.
  </action>
  <verify>Run `npm run build` from apps/command-center — investor page builds successfully. Verify no "window is not defined" errors (all recharts components must be 'use client').</verify>
  <done>Investor dashboard shows all 5 sections with live data, charts render correctly, dark mode toggle works, Print/PDF button calls window.print(), ownership shows 55/30/15 split</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Investor page imports getInvestorMetrics (not placeholder values)
- RevenueTrendChart and ChannelPieChart both have 'use client' directive
- DarkModeToggle uses useTheme from next-themes
- PrintButton calls window.print() with print:hidden class
- Ownership table shows 55/30/15 (not 70/30)
</verification>

<success_criteria>
INVST-01: Revenue trend line chart with monthly revenue + projection lines
INVST-02: Channel diversification pie chart with real sales channel data
INVST-03: Production capacity utilization gauge with progress bar
INVST-04: Financial health cards showing gross margin, net margin, revenue vs projection
INVST-05: Ownership table shows Anthony 55%, Olatunde 30%, Olutomiwa 15%
INVST-06: Dark mode toggle button using next-themes
INVST-07: Export PDF button using window.print() with print CSS isolation
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboards-reporting/07-02-SUMMARY.md`
</output>
