---
phase: 04-order-management
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/command-center/src/app/actions/operator-orders.ts
  - apps/command-center/src/app/(dashboard)/dashboard/orders/page.tsx
  - apps/command-center/src/app/(dashboard)/dashboard/orders/tabs.tsx
  - apps/command-center/src/app/(dashboard)/dashboard/orders/new/page.tsx
  - apps/command-center/src/app/(dashboard)/dashboard/orders/[id]/page.tsx
  - apps/command-center/src/components/orders/OperatorOrderList.tsx
  - apps/command-center/src/components/orders/OperatorOrderForm.tsx
  - apps/command-center/src/components/orders/OrderStatusBadge.tsx
autonomous: true

must_haves:
  truths:
    - "User can create an operator order with customer, channel, location, payment method, and line items"
    - "User can view list of operator orders with status badges in a new tab"
    - "User can view order detail page showing line items, totals, and status"
    - "Order form supports STANDARD, CATERING, and FARMERS_MARKET types with conditional fields"
    - "Orders are created in DRAFT status with auto-generated ORD-YYYY-NNNN number"
  artifacts:
    - path: "apps/command-center/src/app/actions/operator-orders.ts"
      provides: "createOperatorOrder, getOperatorOrders, getOperatorOrderById server actions"
      exports: ["createOperatorOrder", "getOperatorOrders", "getOperatorOrderById"]
    - path: "apps/command-center/src/components/orders/OperatorOrderForm.tsx"
      provides: "Multi-line-item order creation form with useActionState"
    - path: "apps/command-center/src/components/orders/OperatorOrderList.tsx"
      provides: "Responsive table-to-cards order list with status badges"
    - path: "apps/command-center/src/components/orders/OrderStatusBadge.tsx"
      provides: "OperatorOrderStatus color-coded badge component"
    - path: "apps/command-center/src/app/(dashboard)/dashboard/orders/new/page.tsx"
      provides: "Create order page route"
  key_links:
    - from: "apps/command-center/src/components/orders/OperatorOrderForm.tsx"
      to: "apps/command-center/src/app/actions/operator-orders.ts"
      via: "useActionState(createOperatorOrder)"
      pattern: "useActionState.*createOperatorOrder"
    - from: "apps/command-center/src/app/(dashboard)/dashboard/orders/page.tsx"
      to: "apps/command-center/src/app/actions/operator-orders.ts"
      via: "getOperatorOrders() call in server component"
      pattern: "getOperatorOrders"
---

<objective>
Create the operator order CRUD server actions, order creation form with multi-line-item support, order list with a new tab on the orders page, and order detail page. Users can create orders in DRAFT status with customer linkage and channel selection.

Purpose: Enable ORD-01 (create orders from any channel with customer linkage), ORD-07 (track across channels), ORD-08 (farmers market fields), ORD-09 (catering deposit fields). This is the core order creation workflow.
Output: Server actions, form component, list component, order detail page.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-order-management/04-RESEARCH.md
@.planning/phases/04-order-management/04-01-SUMMARY.md
@apps/command-center/src/app/actions/sales.ts
@apps/command-center/src/components/sales/SaleForm.tsx
@apps/command-center/src/components/orders/WebsiteOrderList.tsx
@apps/command-center/src/app/(dashboard)/dashboard/orders/page.tsx
@apps/command-center/src/app/(dashboard)/dashboard/orders/tabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create operator order server actions</name>
  <files>apps/command-center/src/app/actions/operator-orders.ts</files>
  <action>
Create a NEW file `apps/command-center/src/app/actions/operator-orders.ts` (do NOT modify the existing `orders.ts` which handles WebsiteOrder).

Add 'use server' directive at top.

Implement these server actions following the exact pattern from `sales.ts` (prevState: FormState, formData: FormData signature):

1. `createOperatorOrder(prevState, formData)`:
   - Call `verifySession()` from dal.ts
   - Parse formData using `createOperatorOrderSchema.safeParse()` from the new validator
   - On validation failure, return `{ errors: validatedFields.error.flatten().fieldErrors }`
   - Calculate totalAmount from lineItems (sum of quantity * unitPrice)
   - Call `generateOrderNumber()` inside the create
   - Use `db.order.create()` with nested `lineItems: { create: [...] }`
   - Set status to DRAFT, createdById to session.userId
   - For CATERING type: compute `balanceDueDate = subDays(eventDate, 7)` using date-fns if eventDate provided
   - `revalidatePath('/dashboard/orders')`
   - Return `{ success: true, message: 'Order created', orderId: order.id }`

2. `getOperatorOrders()`:
   - Call `verifySession()`
   - Query `db.order.findMany()` with include: customer (select firstName, lastName), channel (select name), location (select name), lineItems (select quantity), _count of lineItems
   - Order by createdAt desc
   - Return the array

3. `getOperatorOrderById(orderId: string)`:
   - Call `verifySession()`
   - Query `db.order.findUnique()` with full includes: customer, channel, location, lineItems (include product), createdBy (select firstName, lastName), approvedBy, invoice
   - Return the order or null
  </action>
  <verify>Run `cd apps/command-center && npx tsc --noEmit` to confirm no TypeScript errors.</verify>
  <done>Three server actions exported from operator-orders.ts. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create order form, list, badge components and wire to pages</name>
  <files>
    apps/command-center/src/components/orders/OrderStatusBadge.tsx
    apps/command-center/src/components/orders/OperatorOrderList.tsx
    apps/command-center/src/components/orders/OperatorOrderForm.tsx
    apps/command-center/src/app/(dashboard)/dashboard/orders/new/page.tsx
    apps/command-center/src/app/(dashboard)/dashboard/orders/page.tsx
    apps/command-center/src/app/(dashboard)/dashboard/orders/tabs.tsx
    apps/command-center/src/app/(dashboard)/dashboard/orders/[id]/page.tsx
  </files>
  <action>
Create `OrderStatusBadge.tsx`:
- Map OperatorOrderStatus values to badge variants: DRAFT (secondary/gray), CONFIRMED (blue), PROCESSING (yellow), SHIPPED (purple), DELIVERED (success/green), COMPLETED (success/green), CANCELLED (destructive/red)
- Use the existing Badge component from `@/components/ui/badge`
- Accept `status: string` prop, render colored badge with status label

Create `OperatorOrderList.tsx` ('use client'):
- Follow the EXACT responsive table-to-cards pattern from `WebsiteOrderList.tsx`
- Desktop table (hidden md:block): columns for Order #, Customer, Channel, Items count, Total, Status (OrderStatusBadge), Date
- Mobile cards (md:hidden): stacked layout with order number, customer, total, status badge
- Each row/card links to `/dashboard/orders/${order.id}`
- Accept orders array as prop (typed from getOperatorOrders return)
- Client-side filtering by status (dropdown) and search by order number

Create `OperatorOrderForm.tsx` ('use client'):
- Use `useActionState(createOperatorOrder, undefined)` pattern
- Use `useFormStatus` for SubmitButton (matching SaleForm.tsx pattern)
- Accept props: channels (SalesChannel[]), products (Product[] with pricingTiers), locations (Location[]), customers (Customer[])
- Form fields: customer select (optional), channel select, location select, payment method select, order type toggle (STANDARD/CATERING/FARMERS_MARKET)
- Dynamic line items section:
  - Start with 1 row: product select, quantity input (inputMode="numeric"), unit price (auto-filled from product's first pricing tier, editable)
  - "Add Line Item" button to add rows, X button to remove
  - Store line items in React useState array
  - Serialize to JSON in a hidden input named "lineItems" before submit
  - Show running total at bottom
- Conditional sections:
  - CATERING: show depositAmount input, eventDate date input
  - FARMERS_MARKET: show eventLocation input, weatherNotes textarea, footTrafficNotes textarea
- Mobile-first: all inputs text-base (16px), touch targets h-11+ (44px)
- On success (state.success), show toast via sonner and redirect to `/dashboard/orders/${state.orderId}` using router.push

Update `tabs.tsx`:
- Add a 4th tab: "Operator Orders" alongside existing Manual Sales, Website Orders, Marketplace Sync
- The new tab renders `OperatorOrderList` component

Update `page.tsx`:
- Import and call `getOperatorOrders()` to fetch data for the new tab
- Pass operator orders data to tabs component
- Also fetch channels, products, locations, customers for potential use

Create `new/page.tsx`:
- Server component that fetches channels (db.salesChannel.findMany), products (with pricingTiers), locations, customers
- Renders `OperatorOrderForm` with fetched data as props
- Add a link/button on the orders page header: "New Order" linking to `/dashboard/orders/new`

Update `[id]/page.tsx`:
- Call `getOperatorOrderById(params.id)`
- If order found (check if it has orderNumber field to distinguish from WebsiteOrder), render operator order detail view:
  - Header: order number, status badge, created date, created by
  - Customer info section (if linked)
  - Line items table: product name, quantity, unit price, total
  - Order totals: subtotal, total
  - Catering section (if CATERING type): deposit amount, event date, balance due date
  - Farmers market section (if FARMERS_MARKET): location, weather, foot traffic
  - Action buttons placeholder area (confirm, ship, etc. - wired in plan 04-03)
- If not an operator order, fall through to existing WebsiteOrder detail rendering
  </action>
  <verify>Run `cd apps/command-center && npx tsc --noEmit` and confirm no errors. Run `npm run build` to verify pages render without SSR errors.</verify>
  <done>New Order tab visible on orders page showing operator orders. /dashboard/orders/new renders form. Creating an order produces DRAFT order visible in list. Order detail page shows full order info.</done>
</task>

</tasks>

<verification>
- Orders page loads with 4 tabs including "Operator Orders"
- /dashboard/orders/new renders the multi-line-item form
- Form validates required fields (channel, location, payment method, at least 1 line item)
- Creating an order shows success toast and redirects to detail page
- Order detail page shows all order info including line items
- TypeScript compiles without errors
- Build completes without SSR errors
</verification>

<success_criteria>
- ORD-01: User can create orders from any channel with customer linkage
- ORD-07: Orders associated with sales channels
- ORD-08: Farmers market orders capture location, weather, foot traffic
- ORD-09: Catering orders capture deposit and event date
- Orders created in DRAFT with auto-generated ORD-YYYY-NNNN number
</success_criteria>

<output>
After completion, create `.planning/phases/04-order-management/04-02-SUMMARY.md`
</output>
